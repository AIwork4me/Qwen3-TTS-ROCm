# Evidence

Verbatim records of the validation runs behind the numbers quoted in the
[README](../README.md) and [`docs/benchmarks.md`](../docs/benchmarks.md).
Everything here was produced on the recorded validation host
(**AMD Ryzen AI Max+ PRO 395 / Radeon 8060S, `gfx1151`, ROCm 7.14.0,
kernel 6.17.0-1032-oem, Python 3.12**) on 2026-08-27 unless noted. Files are
machine-generated command transcripts unless marked otherwise; they are
append-only records, not hand-edited reports.

Reproduce any of them with the commands in the `cmd:` header lines inside
each file, or follow [docs/benchmarks.md](../docs/benchmarks.md) and
[`tests/`](../tests/).

## Core validation

| File | What it is | How it was produced |
|---|---|---|
| `benchmark.json` | Machine-readable RTF benchmark results (host, GPU, torch/HIP versions, per-cell RTF lists) | `scripts/benchmark.py` JSON output |
| `benchmark-run.txt` | Raw stdout+stderr of the same benchmark session, incl. per-call `[bench]` lines and the warmup timings | `scripts/benchmark.py` piped through `tee` |
| `official-parity.txt` | Green transcript of the upstream-parity proof: the stock upstream Gradio demo built around our loader's model object, executed through Gradio's event registry incl. one real synthesis | `pytest tests/test_official_demo_parity.py -m "gpu and requires_download" -v -s` |
| `gpu-suite-2026-08-29.txt` | Full on-GPU suite run (25 passed / 0 failed) after the trust-polish documentation round | `pytest -m gpu -q` on the validation host |
| `gpu-suite-2026-08-29-final.txt` | Full on-GPU suite run of the 25-test era (25 passed / 0 failed in 183 s), after the runtime-wording round (loader announcement + flash-attn guard message) | `pytest -m gpu -q` on the validation host |
| `gpu-suite-2026-08-29-firstuser.txt` | Full CPU + GPU suite after the first-user acceptance round (213 CPU + 25 GPU = 238/238; the 5 new CPU tests cover the F1/F2/F4 fixes) — the run behind the then-current 238/238 count claim (2026-08-29) | `pytest -m "not gpu and not requires_download" -q` then `pytest -m gpu -q` |
| `gpu-suite-2026-09-20.txt` | Full on-GPU suite run after Task 1's 0.6B E2E parity additions (34 passed / 0 failed in 296 s, incl. the new 0.6B CustomVoice + 0.6B Base clone suites, the 1.7B suites, and the upstream-parity proof) — the GPU half of the 250/250 count claim as of 2026-09-20 (superseded: current claim 351/351 = 313 CPU + 38 GPU; the latest full 38-test GPU run is recorded in `docs/superpowers/reports/task-5-verification.md`) | `pytest -m gpu -q` on the validation host |
| `first-user-journey-2026-08-29.txt` | Compiled summary of the first-time-user acceptance round: blind README journey on a fresh public clone, timings, network-resilience observations, findings→fixes→tests mapping, clean-room retest verdict (2026-08-29; hand-compiled, marked as such) | live acceptance test; scratch transcripts listed in its PROVENANCE section |
| `env-check.txt` | `qwen3-tts-rocm-check` environment self-check output (bilingual) on the validation host | `qwen3-tts-rocm-check` |
| `models-dl.txt` | Six-repository download log with per-alias provenance (ModelScope primary channel proven; sizes match `scripts/download_models.sh --help`) | `bash scripts/download_models.sh <alias>` per alias |
| `install-run.txt` | `scripts/install.sh` run record (pinned ROCm wheel stack + editable install) | `bash scripts/install.sh` |
| `load-smoke.txt` | All-six-model load/unload GPU smoke | `pytest tests/test_loader_all_models.py -m "gpu and requires_download" -v -s` |
| `tokenizer-codec.txt` | Official 12Hz tokenizer encode→decode roundtrip + getters on GPU | `pytest tests/test_tokenizer_codec.py -m "gpu and requires_download" -v -s` |
| `gen-customvoice.txt` / `gen-voicedesign.txt` / `gen-voiceclone.txt` | Full on-GPU generation suites for the three synthesis paths | `pytest tests/test_generate_custom_voice.py` / `test_voice_design.py` / `test_voice_clone_workflow.py` `-m "gpu and requires_download" -v -s` |
| `demo-smoke.txt` | Live five-tab demo smoke: HTTP boot, one real REST synthesis, teardown orphan-check | `qwen3-tts-rocm-demo` + `curl` (steps documented inside) |
| `demo-rest-gen-zh.wav` | The 5.52 s clip from that smoke (text `你好，欢迎体验 Qwen3 TTS 的 ROCm 演示。`, speaker Vivian, `custom-voice` 1.7B) — the single curated audio sample tracked in git as an exception to the `*.wav` ignore policy; linked from the README | generated by `demo-smoke.txt` step [3] |
| `run-demo-expected-failure.txt` | The record that established the parity hypothesis (why the unmodified upstream demo initially failed here); cited by `tests/test_official_demo_parity.py` | manual run transcript |
| `spike/spike-report.md` | Day-one feasibility spike: verdict, exact wheel stack, versions | manual probe session |
| `spike/gpu-probe.txt` | The spike's GPU sanity probe (`SPIKE-GPU-OK`) | `/tmp/spike_probe.py` (script in report) |
| `spike/tokenizer-smoke.txt` | The spike's tokenizer smoke (`TOKENIZER-OK`) | manual probe session |
| `docker-build-final.txt` | Final Docker image build log (`EXIT=0`; ≈ 2 GB of ROCm wheels downloaded). Post-build `docker image inspect .Size` on the same host: 2,466,485,851 bytes (≈ 2.47 GB compressed content) — metric details in [`docker/README.md`](../docker/README.md) | `docker build` per [`docker/README.md`](../docker/README.md) |
| `docker-gpu-gen-2026-08-30.txt` | Real in-container GPU synthesis: image rebuilt from main, container run with `/dev/kfd`+`/dev/dri` passthrough and mounted `models/`, one custom-voice REST synthesis (6.24 s WAV, `Finished`), in-container self-check `HIP available · 1 GPU (gfx1151) · models 6/6`; image content size 2,466,515,335 bytes ≈ 2.47 GB (2026-08-30) | `docker build` + `docker run` + demo REST API per [`docker/README.md`](../docker/README.md) |
| `docker-ci.txt` | Docker + CI validation ladder of 2026-08-27 — the offline-equivalent checks run before the repository had a GitHub remote (superseded for CI by the live green [Actions runs](https://github.com/AIwork4me/Qwen3-TTS-ROCm/actions); kept as the Docker validation record) | `docker build` + probe script |
| `ground-truth-2026-09-20.md` | Ground-truth audit for the P0 capability-parity program: project/upstream SHAs, installed versions, upstream `finetuning/` layout (entry points, CLI flags, defaults, checkpoint conversion), runtime `get_supported_languages()` list, 0.6B CustomVoice `instruct` behavior (silently ignored), evidence inventory (2026-09-20; read-only audit, facts verbatim) | read-only audit; commands recorded inside |
| `ci-2026-09-21-8815238.txt` | First verified CI run on the P0 suite: push `8815238`, run 35526426415 — `test (3.10/3.11/3.12)` + `build` all success, `251 passed, 1 skipped, 38 deselected` per Python job (gh-captured transcript, 2026-09-21) | machine-captured via `gh run list` / `gh api` |
| `upstream-issue-2026-09-21.txt` | Upstream issue filed: QwenLM/Qwen3-TTS#372 — `sft_12hz.py` hard-codes `flash_attention_2`, breaking official fine-tuning on flash-attn-less stacks (ROCm); gh-captured issue metadata (2026-09-21) | machine-captured via `gh issue view` |
| `vllm-omni-feasibility-2026-09-21.txt` | vLLM-Omni offline-inference feasibility check (roadmap rung 1), verdict **FEASIBLE — single configuration**: phase-marked transcript — upstream-docs inspection (pinned README vLLM section + vllm-omni ROCm install doc, verbatim commands), disk/env precheck (1085 GB free, gfx1151, host ROCm 7.2.1), install of the documented stack (`vllm==0.28.0+rocm723` + `vllm-omni==0.28.0` + `onnxruntime-rocm==1.22.2.post3`) into the isolated gitignored `.work-vllm/venv` (validated `.venv` untouched), platform probes (wheel embeds gfx1151 code objects; `RocmPlatform` → `AMD_Radeon_8060S`), and two runs of the byte-unmodified documented example `end2end.py --query-type CustomVoice` (cold 55.3 s / warm 19.0 s generation, `PYTHON_EXIT_CODE=0`, finite non-silent 6.0 s / 24 kHz WAV). Scope guard: one example, single config, no serving/perf/quality claims | install + example run piped through `tee`; full run-2 log kept in `.work-vllm/run2.log` |
| `vllm-omni-feasibility-2026-09-21.json` | Machine-readable companion of the same check: verdict, upstream doc paths (files+sections), per-phase commands with exit codes and key excerpts, run evidence (waveform sanity, timings), env (python 3.12.3, vllm `0.28.0+rocm723`, vllm-omni 0.28.0, torch `2.12.0+git6bbd260`/HIP 7.2.53211, gfx1151, 1085 GB free), meta (date, git HEAD `25f5436…`, upstream SHA `022e286b…`) | written alongside the transcript |
| `benchmark-06b-2026-09-20.json` | Machine-readable 0.6B RTF benchmark (2026-09-20): both 0.6B checkpoints, 4 cells x 2 runs each, median RTF 1.03–1.30 (`custom-voice-0.6b`, load 4.0 s, peak 2.80 GiB) and 1.19–1.26 (`base-0.6b`, load 1.5 s, peak 3.10 GiB); per-cell records carry RTF/wall/audio lists plus `load_seconds`/`peak_alloc_gb`; meta carries `git_head` + audited upstream SHA | `scripts/benchmark.py --aliases custom-voice-0.6b,base-0.6b --json-out ...` |
| `benchmark-06b-2026-09-20.txt` | Verbatim stdout+stderr transcript of the same 0.6B benchmark session (per-call `[bench]` lines, load/warmup/peak timings, markdown table, `exit_code=0`) | same command piped through `tee` |
| `multilingual-matrix.json` | Machine-readable multilingual capability matrix (2026-09-20): all 10 officially supported languages exercised end to end on the Radeon GPU — 10 CustomVoice + 10 VoiceDesign cells + 4 representative cross-lingual clone pairs (plus the 4 clone-reference generations), every cell `pass: true` from waveform sanity (finite, non-silent, valid sample rate, bounded duration); rows carry the resolved runtime `language_id`, wall/audio timings and checks; meta carries git HEAD, audited upstream SHA, per-section model alias and the runtime `get_supported_languages()` list. NOT a pronunciation-quality claim; clone coverage representative, not exhaustive (24/24 matrix cells, 152.6 s wall) | `scripts/validate_languages.py` JSON output |
| `multilingual-matrix.txt` | Verbatim stdout+stderr transcript of the same multilingual validation run (commented env header — timestamp, GPU, torch/HIP/qwen-tts versions, git HEAD, upstream SHA, manifest, `max_new_tokens=512`, claim wording; `[phase]` load/unload lines; per-cell `[cv]`/`[vd]`/`[cv-ref]`/`[clone]` lines; summary table + caption; closing `[summary] matrix cells: 24/24 …` / `[summary] RESULT: PASS` / `[evidence] wrote …` lines — no shell exit-code token is recorded, the run's success marker is the `[summary] RESULT: PASS` line) | same command piped through `tee` |
| `voice-workflow-2026-09-20.json` | Machine-readable three-phase timings for the Voice Design → reusable-voice workflow (2026-09-20): `design_s=5.469` / `prompt_s=0.307` / `reuse_s_1=5.283` / `reuse_s_2=6.417` — the phases recorded SEPARATELY, never collapsed; every generation `max_new_tokens=512`; meta carries the env header (Radeon 8060S `gfx1151`, torch `2.12.0+rocm7.14.0`, hip `7.14.60850`, qwen-tts `0.1.1`), git HEAD `2a90229…` and the audited upstream SHA `022e286b…` | written by the walk-through python block of the same driver run |
| `voice-workflow-2026-09-20.txt` | Verbatim transcript of the same session in two machine-generated parts: the GPU workflow suite (`tests/test_voice_workflow.py -m gpu -v -s`, 4 passed / `PYTEST_EXIT=0` — design+preview, two-sentence reuse, official-payload save/load roundtrip, official schema) and, behind a marked separator, the appended design → save → load → reuse×2 walk-through (`[walk]` lines ending `WALK-OK`, `WALK_EXIT=0`); the walk persists into a throwaway temp voices dir so the repo tree stays clean | `pytest … \| tee` + `python - <<'PY' … >>` from the evidence driver shell run |
| `finetune-smoke-2026-09-20.json` | Machine-readable fine-tuning execution-smoke facts (2026-09-20): the OFFICIAL upstream fine-tuning workflow run end-to-end on the Radeon GPU — self-generated 12-utterance dataset, `prepare_data.py` AS-IS (exit 0), 12 optimizer steps of `sft_12hz.py` on 1.7B Base bf16 (wall 19.0 s, peak 18.02 GiB, loss lines quoted verbatim), per-epoch checkpoints, reload via `Qwen3TTSModel.from_pretrained` + sane `generate_custom_voice` synthesis (24 kHz, 3.52 s, non-silent). Scope guard inside: execution validation only — explicitly NOT speaker-similarity/convergence/quality claims. Also records the verbatim flash-attn `ImportError` of the AS-IS run, the exact one-line disclosed `sdpa` deviation applied inside the gitignored `.upstream` clone, its restore (`status --porcelain` empty), and the network-forced pinned-content bootstrap (github.com:443 blocked; SHA verified via api.github.com, content via codeload, commit object SHA-identical to the pinned upstream SHA) | read from the structured run facts; every value traces to the `.txt` transcript |
| `finetune-smoke-2026-09-20.txt` | Verbatim phase-marked transcript of the same fine-tuning smoke (phases 4.1–4.6): upstream pin attempts, pinned-content bootstrap proofs (37-blob hash match, tree match, commit-SHA reproduction), `finetuning/` inspection recorded BEFORE any run, dataset generation `[render]`/`[utt]` lines, `prepare-exit=0` + `SCHEMA-OK`, the AS-IS flash-attn failure traceback, the one-line diff, `Epoch … Loss` lines, launcher CUDA stats, `RELOAD-AND-SYNTHESIS-OK`, zero-patch audit (`[status lines: 0]`). Note: MIOpen `*_grid_desc` kernel-tuning lines were filtered from the live stream of phases 4.4–4.6 by the recorded `grep -v` in each command itself (nothing was edited after the fact) | every command piped through `tee` from the Task 4 driver shell |
| `streaming-2026-09-21.txt` | True-streaming probe transcript (2026-09-21), verdict `streaming_exposed=False`: qwen-tts 0.1.1's official API returns audio only at completion. Machine-captured API-surface header (inspect signatures, generator=False for all three generate_* methods, verbatim `non_streaming_mode` docstring lines, needle-searched file:line quotes), then 4 scenarios x 2 runs on the CustomVoice 1.7B path (short 17 chars / long 205 chars, both `non_streaming_mode` values, `max_new_tokens` 512/768, do_sample=True temperature=1.0): every run `chunks=1` with `ttfb==wall` (short 3.4–4.0 s, RTF 1.22–1.25; long 66–79 s wall for 52–59 s audio, RTF 1.28–1.34), `underruns=0 starved=0.00s` (whole buffer exists at playback start). MIOpen kernel-tuning lines are present verbatim (unfiltered capture); the `grep -E "^\[probe\]|^# "` digest is the run summary | `scripts/streaming_probe.py --json-out evidence/streaming-2026-09-21.json` piped through `tee` |
| `streaming-2026-09-21.json` | Machine-readable companion of the same probe: `streaming_exposed=false`, full `api_surface` block (package/version/wrapper file, per-method signatures + generator checks + verbatim doc lines, codec-decode signature, 7 file:line key quotes), per-scenario per-run records (`ttfb_s`, `chunk_count`, `cadence_s`, `total_wall_s`, `audio_s`, `rtf`, `underrun_events`, `starved_s`), conclusion, env (Radeon 8060S `gfx1151`, torch `2.12.0+rocm7.14.0`/HIP `7.14.60850`, qwen-tts `0.1.1`), meta (date, git HEAD `1b9c197…`, upstream SHA `022e286b…`, alias/runs/sampling/warmup policy) | written by the same probe run |
| `benchmark-2026-09-21.json` | Machine-readable 1.7B cross-day replication RTF benchmark (2026-09-21, Step E): all three 1.7B aliases, 12 cells x 2 runs, median RTF 1.21–1.52 (`custom-voice` 4.5 s load / 4.79 GiB peak; `voice-design` 2.0 s / 4.85 GiB; `base` 1.9 s / 5.01 GiB); meta carries `git_head 6df5e86…` + audited upstream SHA; deltas vs the 2026-08-27 baseline session tabulated in `docs/benchmarks.md` | `scripts/benchmark.py --aliases custom-voice,voice-design,base --json-out ...` |
| `benchmark-2026-09-21.txt` | Verbatim stdout+stderr transcript of the same 1.7B replication session (per-call `[bench]` lines; warmups 5.8/4.9/9.7 s — the base degenerate warmup loop did not recur; markdown table) | same command, stdout captured to log and copied verbatim |
| `benchmark-06b-2026-09-21.json` | Machine-readable 0.6B cross-day replication RTF benchmark (2026-09-21, Step E): both 0.6B checkpoints, 8 cells x 2 runs, median RTF 1.04–1.23 (`custom-voice-0.6b`, load 4.1 s, peak 2.78 GiB) and 1.15–1.23 (`base-0.6b`, load 1.5 s, peak 3.05 GiB); day-over-day deltas vs `benchmark-06b-2026-09-20.json` (−8.7%…+2.5% RTF, ±3.5% load/peak) tabulated in `docs/benchmarks.md`; meta carries `git_head 6df5e86…` | `scripts/benchmark.py --aliases custom-voice-0.6b,base-0.6b --json-out ...` |
| `benchmark-06b-2026-09-21.txt` | Verbatim stdout+stderr transcript of the same 0.6B replication session (per-call `[bench]` lines, load/warmup/peak timings, markdown table) | same command, stdout captured to log and copied verbatim |
| `reference-closure-ground-truth-2026-09-21.md` | Ground-truth baseline for the v0.2 reference-closure program (Task 0, read-only): both HEAD SHAs (downstream `558d452…`, upstream pinned `022e286b…` == `ls-remote` HEAD, no drift), versions (qwen-tts 0.1.1 installed / pyproject 0.2.0 / release v0.1.0), #372 state `OPEN` with 0 comments, test collection counts (267 CPU + 38 GPU = 305, collect-only — the frozen Task-0 baseline; superseded by later same-day suite growth to 313 CPU + 38 GPU = 351 per the 2026-09-21 claims audit), README capability-matrix rows, evidence inventory, and the verbatim 161-line `finetuning/sft_12hz.py` at the pinned SHA (root-cause line 51 `attn_implementation="flash_attention_2"` still present) | read-only audit; commands recorded inside |
| `upstream-372-pristine-failure-run1.txt` | Task 1 pristine double reproduction, run 1 of 2 (2026-09-21): the validated 2026-09-20 fine-tune invocation re-run UNCHANGED against byte-pristine upstream (`022e286b…`, `status --porcelain` empty before AND after) in a fresh process with a fresh dataset (`.work-finetune/repro-r1/`, 12 utterances self-generated + `prepare_data.py` AS-IS, both exit 0) — upstream `sft_12hz.py` AS-IS fails at model init with exit 1 and the flash-attn `ImportError` matching the 2026-09-20 capture byte-for-byte (`CLASS-MATCH-VERBATIM` via in-transcript `diff`); rocm-smi snapshots wrap every GPU command | driver shell (`.work-finetune/repro-r1-driver.sh`) piped through `tee`; archived byte-identical (sha256 `0504afa7…`) |
| `upstream-372-pristine-failure-run2.txt` | Task 1 pristine double reproduction, run 2 of 2 (2026-09-21): identical procedure in a SEPARATE fresh process with fresh dirs (`.work-finetune/repro-r2/`) — same pristine upstream SHA before/after, dataset prep exits 0, `sft_12hz.py` AS-IS exit 1 with the same flash-attn `ImportError` (`CLASS-MATCH-VERBATIM`); the run-1 vs run-2 sft stdout streams are byte-identical (`RUN1-RUN2-SFT-STDOUT-IDENTICAL`, verified at archive time), and both exit codes are the python process's own (`PIPESTATUS[0]`, not a pipeline wrapper — avoiding the `as-is-run-exit=0` errata of the 2026-09-20 smoke) | driver shell (`.work-finetune/repro-r2-driver.sh`) piped through `tee`; archived byte-identical (sha256 `6db78216…`) |
| `upstream-372-root-cause.md` | Task 2 root cause + controlled isolation (2026-09-21): the causal chain for QwenLM/Qwen3-TTS#372 (hard-coded `attn_implementation="flash_attention_2"` at `sft_12hz.py:51` → transformers init-time dispatch check ImportError on the flash-attn-less ROCm stack), the 8 brief questions answered from source with file:line citations, and the controlled experiment — the Task 1 failing invocation re-run against the FIX worktree (`.upstream/Qwen3-TTS-fix`, branch `fix/finetuning-attn-implementation` @ pinned SHA `022e286b…`) whose ONLY delta is the one token `flash_attention_2`→`sdpa`: full chain passes fresh (prep → `prepare_data.py` → direct `python sft_12hz.py` exit 0, 12 optimizer steps, 2 checkpoints → official-API reload resolving `_attn_implementation='sdpa'` → synthesis, 24 kHz / 4.00 s / finite / non-silent) — **no second blocker**; alternative hypotheses rejected; remaining uncertainties scoped; worktree restored pristine afterwards (Task 3 re-commits the change). Verbatim transcript kept in `.work-finetune/iso-driver.log` (sha256 `05be3ad7…`); recording-level errata disclosed inside. Appendix (2026-09-21): the upstream PR #373 submission record — OPEN at submission, fork `AIwork4me:fix/finetuning-attn-implementation`, commits `0be0026` + `48b8644` on base `022e286` (the pinned SHA), with the verbatim `gh pr view` verification | driver shell piped through `tee`; excerpts embedded verbatim |
| `upstream-372-round-a-loads1.txt` | Task 5 Round A targeted loader validation, run 1 of 3 (2026-09-21, brief 6A): fresh process loads the 1.7B Base checkpoint (`models/Qwen3-TTS-12Hz-1.7B-Base`, the smoke's `init_model_path`) exactly as patched `sft_12hz.py` does — via the FIX worktree's own symbols (`build_arg_parser()` + `Qwen3TTSModel.from_pretrained`, bf16, `attn_implementation="sdpa"`; dummy `--train_jsonl`/`--output_model_path` per the disclosed parser-args plan defect, never opened) — printing `parsed attn_implementation=sdpa`, `config._attn_implementation=sdpa`, `cuda_available=True`, `device=AMD Radeon 8060S Graphics`, `ROUND_A_LOAD_OK`, `python-exit=0`; no FlashAttention ImportError (only the benign qwen-tts import warning banner); `mem_alloc_GiB=0.00` — the loader call has no `device_map`, so the load is CPU-resident by construction (matches the Task 2 phase-C2 probe: `device: cpu`, `0.000 GiB`), and VRAM is flat across the wrapping rocm-smi snapshots; FIX worktree `48b8644…` pristine before/after, downstream `5bfd448…`. **Fix-round-1 supplement appended below (same file, append-only diff 111+/0-):** a clearly-labeled "SUPPLEMENT (FIX ROUND 1): GPU-RESIDENT VARIANT" section — same driver plus `device_map="cuda:0"` (wrapper accepted it: `placement_method=device_map`, `.to()` fallback not needed) printing `param_device=cuda:0`, `mem_alloc_GiB=3.91` (GPU-resident), `ROUND_A_LOAD_OK`, own-exit 0 — proving "ROCm GPU residency is real" per the ruling | `bash .work-finetune/round_a_driver.sh 1` piped through `tee` (verbatim section sha256 `d902f7a9…`); supplement via `bash .work-finetune/round_a_driver_gpu.sh 1` piped through `tee -a` (file sha256 after append `5ab46949…`) |
| `upstream-372-round-a-loads2.txt` | Task 5 Round A targeted loader validation, run 2 of 3 (2026-09-21): identical procedure in a SEPARATE fresh process (start 14:21:50, distinct from run 1's 14:21:38), same pinned inputs (FIX worktree `48b8644…`, `status --porcelain` 0 lines; driver sha256 `fa87b054…`) — all criteria print PASS (`parsed`/`config` `=sdpa`, `cuda_available=True`, Radeon 8060S, `ROUND_A_LOAD_OK`, `python-exit=0`), no FlashAttention ImportError, `mem_alloc_GiB=0.00` per the run-1 note (CPU-resident by construction), rocm-smi snapshots before/after. **Fix-round-1 supplement appended below (same file, append-only 111+/0-):** GPU-resident variant (driver sha256 `a6cd725a…`) in its own fresh process (start 14:26:41) — `placement_method=device_map`, `param_device=cuda:0`, `mem_alloc_GiB=3.91`, `ROUND_A_LOAD_OK`, own-exit 0, rocm-smi before/after | `bash .work-finetune/round_a_driver.sh 2` piped through `tee` (verbatim section sha256 `e6f520b1…`); supplement via `bash .work-finetune/round_a_driver_gpu.sh 2` piped through `tee -a` (file sha256 after append `7d1f0050…`) |
| `upstream-372-round-a-loads3.txt` | Task 5 Round A targeted loader validation, run 3 of 3 (2026-09-21): identical procedure in a third separate fresh process (start 14:21:58) — all criteria print PASS as in runs 1–2, `python-exit=0`, no FlashAttention ImportError, `mem_alloc_GiB=0.00` (same by-construction note), rocm-smi snapshots before/after; together the three transcripts show 3 distinct process invocations, the brief 6A Step 5.2/5.3 criteria all met. **Fix-round-1 supplement appended below (same file, append-only 111+/0-):** GPU-resident variant in its own fresh process (start 14:26:51) — `placement_method=device_map`, `param_device=cuda:0`, `mem_alloc_GiB=3.91`, `ROUND_A_LOAD_OK`, own-exit 0; verbatim-path sections in all three files stay byte-untouched above their supplements | `bash .work-finetune/round_a_driver.sh 3` piped through `tee` (verbatim section sha256 `48e8f2fb…`); supplement via `bash .work-finetune/round_a_driver_gpu.sh 3` piped through `tee -a` (file sha256 after append `5e3e9cfc…`) |
| `upstream-372-e2e-run1.txt` | Task 6 Round B, E2E fine-tuning run 1 of 2 from the FIX worktree (2026-09-21, brief 6B): the patched official `sft_12hz.py` invoked DIRECTLY as a script (`cd .upstream/Qwen3-TTS-fix && python finetuning/sft_12hz.py --attn_implementation sdpa …`, validated 2026-09-20-smoke scale/args, 1.7B Base, batch 2, lr 2e-5, 2 epochs, speaker `e2e_r1_speaker`) executes the whole workflow from a fresh tree (`.work-finetune/e2e-r1/`, created after `ls` proved non-existence): 12-utterance self-generated dataset re-rendered in-tree (`generator-exit=0`) → `prepare_data.py` from the FIX worktree (diff vs pristine prints `IDENTICAL`; `prepare-exit=0`) → training exit 0 (`PIPESTATUS[0]`, not the tee wrapper), 12 optimizer steps, loss lines verbatim `14.6621`→`10.0124`, wall 22.8 s → per-epoch checkpoints (`CKPT-CONFIG-OK`, epoch-1 safetensors sha256 `bf87ab9d…`) → official `qwen_tts` API reload + synthesis (`RELOAD-AND-SYNTHESIS-OK`, 24 kHz, 3.20 s, finite, non-silent, peak 0.3320, rms 0.05800). Peak memory via the disclosed in-process launcher re-run of identical args (a direct invocation cannot expose `torch.cuda.max_memory_allocated`; `reset_peak_memory_stats` at start): `18.013 GiB` allocated / `18.477 GiB` reserved; which invocation produced what is stated in-transcript and in the JSON. FIX worktree `48b8644…` pristine before AND after (`status --porcelain` 0 lines); every GPU phase wrapped with rocm-smi snapshots. Scope guard inside: execution validation only, no quality claims | `bash .work-finetune/e2e-driver.sh 1` piped through `tee` (archived byte-identical, sha256 `68156fbb…`) |
| `upstream-372-e2e-run1.json` | Machine-readable companion of the same run: environment (gfx1151, ROCm 7.14.60850, torch 2.12.0+rocm7.14.0, qwen-tts 0.1.1, Python 3.12.3), code-under-test block (FIX branch/HEAD `48b8644…`, pristine before/after, `git diff 022e286..HEAD --stat` as the only-difference-vs-pristine proof, `prepare_data.py` identity), independence block (fresh tree, 14:43:54+08:00 start, dataset re-prepared in-tree), per-phase commands with exits, per-invocation metrics (steps/loss/wall/peak-mem/checkpoint sha256), reload+synthesis waveform checks, scope guard | written alongside the transcript |
| `upstream-372-e2e-run2.txt` | Task 6 Round C, E2E fine-tuning run 2 of 2 (2026-09-21, brief 6C): identical procedure REPRODUCED fully fresh — separate driver process started 14:46:38+08:00 (after run 1 completed), disjoint tree `.work-finetune/e2e-r2/` (non-existence proven first), dataset RE-RENDERED into its own tree (concatenated wav-bytes sha256 identical to run 1 by deterministic per-render reseed `1234`, while `train_raw.jsonl` hashes differ because each run's records point into its own tree — regenerated, never copied) → `prepare-exit=0` → DIRECT `sft_12hz.py --attn_implementation sdpa` exit 0 (`PIPESTATUS[0]`), 12 optimizer steps, loss `14.1067`→`11.9973` (differs from run 1 — unseeded shuffle order, genuinely separate trainings; all four epoch-1 safetensors sha256 across the two runs are distinct), wall 23.2 s → checkpoints (`spk_id={'e2e_r2_speaker': 3000}`) → official-API reload + synthesis (`RELOAD-AND-SYNTHESIS-OK`, 24 kHz, 3.44 s, finite, non-silent, peak 0.1914, rms 0.04274); launcher peak-mem `18.026`/`18.496 GiB`; FIX worktree pristine before/after; rocm-smi snapshots around every GPU phase | `bash .work-finetune/e2e-driver.sh 2` piped through `tee` (archived byte-identical, sha256 `6877ad19…`) |
| `upstream-372-e2e-run2.json` | Machine-readable companion of run 2: same structure as run 1's JSON plus the separation-from-run-1 record (timestamps, disjoint trees, fresh dataset, no reused checkpoint/trainer state) and the four-distinct-trained-weights-fingerprints fact; scope guard inside | written alongside the transcript |
| `upstream-372-default-semantics.txt` | Task 7 Round D default-configuration semantics proof (2026-09-21, brief 6D), one transcript, two legs with NO `--attn_implementation` flag: (a) UNIT — the Task 4 unittest suite on the FIX branch, 5 tests OK / exit 0, incl. `test_default_is_flash_attention_2` and `test_default_value_reaches_model_loader` (default parse AND default reaching `from_pretrained` are `flash_attention_2`); (b) RUNTIME — a real default-flag load of 1.7B Base in a fresh process (`.work-finetune/round_d_default_load.py`: parser default travels into the real `Qwen3TTSModel.from_pretrained`, bf16) fails on this flash-attn-less ROCm host with python-exit 1 and the `ImportError: FlashAttention2 … flash_attn seems to be not installed` traceback, `CLASS-MATCH-VERBATIM` vs the pristine-upstream capture via in-transcript `diff` against `upstream-372-pristine-failure-run1.txt`; header carries python 3.12.3, torch `2.12.0+rocm7.14.0` / HIP 7.14.60850, qwen-tts 0.1.1, transformers 4.57.3, Radeon 8060S `gfx1151`, an explicit `flash_attn=ABSENT` probe, FIX worktree `48b8644…` pristine before/after; rocm-smi snapshots wrap the GPU-touching attempt; every recorded exit is the process's own. Closes `ROUND_D_RESULT=PASS` and the required verbatim statement `Default configuration semantics preserved; CUDA runtime was not independently tested.` — CUDA runtime explicitly NOT claimed | `bash .work-finetune/round_d_driver.sh 2>&1 \| tee` (archived byte-identical, sha256 `0e6c1723…`) |
| `upstream-372-diff-audit.txt` | Task 7 Round E per-hunk audit of the FULL FIX-branch diff `022e286..48b8644` (2026-09-21, brief 6E; 151 lines, 4 hunks, 2 files; diff saved at `.work-finetune/fix-branch.diff`; two-dot vs three-dot proven byte-identical — linear descendant). Analysis document over machine-captured range checks: exactly the 2 expected commits in range (patch `0be0026` `sft_12hz.py` 10+/5−, tests `48b8644` new file 105+), whitespace-ignoring stat identical (zero formatting churn), prohibited-pattern scan over changed lines 0 hits (no AMD/ROCm branching, no `torch.version.hip`, no `device_map`, no downstream branding), imports added are stdlib-only (no new dependencies), all 5 deletions + 115 insertions itemized (3 verbatim moves of the `train()` header, 1 replaced-equivalent parse, the one call-site `args.attn_implementation` change, the new flag defaulting to the UNCHANGED `flash_attention_2`, the test file); per-hunk verdicts PASS ×4, `DIFF_IS_MINIMAL_AND_GENERIC — PASS`; the diff itself appended byte-identically | `bash .work-finetune/round_e_audit_driver.sh 2>&1 \| tee` |
| `gpu-ci-prep-validation.txt` | GPU CI preparation validation (Task 11, 2026-09-21, prepare-only): `yaml.safe_load` + structural assertions on `.github/workflows/gpu-nightly.yml` (schedule cron `0 2 * * *` + `workflow_dispatch(suite: gpu-short\|full-weekly)`, no pull_request/push trigger, both jobs on `[self-hosted, radeon-gfx1151]`, timeouts 120/720, checkout pinned to the ci.yml SHA, `permissions: contents: read`), the full 38-node `-m gpu --co -q` inventory, per-filter collection with node lists proving the three nightly `-k` slices select 6 + 22 + 4 = 32 real disjoint nodes, the 6-node complement (loader smokes + fixture smoke → full-weekly only), the brief-draft filters checked and why they were refined (`parity` over-matches, `studio` and `validate_languages` select 0 GPU nodes), and the `--json-out` flag check; STATUS header: BLOCKED ON RUNNER INFRASTRUCTURE, nothing executed on a GitHub runner | validation driver shell piped into the file; every `cmd:` line reproducible |
| `vllm-vs-qwen-tts-2026-09-21.json` | Machine-readable controlled A/B, qwen-tts vs vLLM-Omni on the same 8-prompt CustomVoice workload and the SAME `Qwen3-TTS-12Hz-1.7B-CustomVoice` checkpoint directory (Task 12, 2026-09-21): block order A-B-A (qwen A1 → vLLM-Omni B → qwen A2; deviation from the brief's literal ABBA disclosed in-file), 3 timed runs per prompt per block — 72 rows total (48 qwen + 24 vllm), **zero failures**, every waveform finite/non-silent @24 kHz. Headline medians: RTF qwen **1.303** vs vLLM-Omni **1.182** (vLLM faster on all 8 prompts, ratios 0.846–0.986); model load **4.8 s vs 69.1 s**; whole-stack GPU memory peak (amdgpu vram+gtt sysfs, identical 1 Hz sampler both sides) **~7.0–7.1 GiB vs ~26.3 GiB**. Carries environment blocks for BOTH stacks, per-prompt per-side medians with ratio, per-block load/warmup/memory summaries, block-boundary rocm-smi/sysfs snapshots proving full unload between blocks, fairness notes (max_new_tokens=512 enforced qwen-side but the field is not consumed by the vllm-omni qwen3_tts pipeline — no run on either side reached any cap; per-stack default sampling), explicit unsupported-parity list, and the continuous 1108 s run window (all block exits 0). NO quality claims (Task 14 scope) | gitignored drivers `.work-vllm/ab_prompts.json` + `ab_qwen.py` + `ab_vllm.py` + `ab_blocks.sh`, aggregated by `.work-vllm/ab_aggregate.py` |
| `vllm-vs-qwen-tts-2026-09-21.txt` | Verbatim transcript of the same A/B session (10772 lines): baseline/pre/post block rocm-smi + sysfs snapshots (GTT back to ~140 MiB idle between every block), full engine init logs of both stacks, per-run `[ab-qwen]`/`[ab-vllm]` wall/audio/RTF lines, per-block SUMMARY JSON lines, `A/B RUN END overall_rc=0` | `bash .work-vllm/ab_blocks.sh` piped through redirect (log copied verbatim) |
| `quality-benchmark-2026-09-21.txt` | Quality-benchmark foundation transcript (Task 14 / brief 15, 2026-09-21, 1108 lines incl. the fix-round-1 correction block): Step 14.1 investigation gates — PyPI/GitHub-API license probes for all five metric candidates captured before any verdict, the four-gate verdict table decided BEFORE installing anything into `.venv` (**ACCEPT**: jiwer 4.0.0 Apache-2.0; resemblyzer 0.1.4 Apache-2.0 — the brief's "MIT" corrected at execution; openai-whisper 20250625 MIT; pystoi 0.4.1 MIT. **REJECT**: pesq 0.0.4 — the MIT-labelled wrapper bundles the ITU-T P.862 reference C code whose own IPR notice (owners BT/Psytechnics + KPN/OPTICOM, no commercial use/distribution) is quoted verbatim in the appended fix-round-1 correction block, with sdist/source sha256 provenance; Section A's own capture step had failed on the sdist layout and the failure is recorded in place), scratch-venv installs + smokes (jiwer hand-computed WER/CER; pystoi synthetic pair; resemblyzer CPU embedding cosine 1.000 identical vs 0.322 different tones; whisper tiny GPU probe PASS on gfx1151 — 5.00 s GPU vs 4.81 s CPU on the 2.8 s bundled clip — plus the same probe on CPU), then the `.venv` install of only the ACCEPTed four with import verification, `pip check` clean, and pesq-absence proofs (rc 1). Two probe-script failures (jiwer `__version__` attr; `pystoi.stoi` module/function shadowing) are recorded with their RC=1 lines and corrected follow-ups — package problems were ruled out in both cases. Step 14.5 evidence run appended (rocm-smi before/after, rc 0); the correction block also states the runtime-resolved ASR device for that run (cuda) against the JSON's policy-string `meta.asr.device`. Gate scratch lives in gitignored `.work-quality/` | `bash .work-quality/gates.sh` + `install_venv.sh` + `evidence_run.sh` + `fix1_ipr_correction.sh`, each piped through `tee -a` |
| `quality-benchmark-2026-09-21.json` | Machine-readable quality benchmark on the fixed 5-case manifest (`tests/data/quality_benchmark_manifest.json`, seed 20260921): per-case CER (zh, jiwer character-level after whitespace+punctuation normalization) / WER (en, word-level after lowercase+punctuation normalization) against whisper-tiny transcripts (greedy, language-pinned), verbatim transcripts, audio seconds, and for the clone case the resemblyzer speaker-embedding cosine ref-vs-output. Values recorded verbatim with NO quality interpretation (the harness measures, humans interpret — e.g. the cross-lingual clone case's CER 2.8 reflects a whisper-tiny transcript of synthetic-babble-referenced Chinese audio; the known-limitation case, see `tests/test_voice_clone_workflow.py` docstring). `omitted_metrics` records the pesq REJECT reason and the STOI scope note (pystoi ACCEPTed but not computable for text-conditioned cases). Sampling mirrors `scripts/benchmark.py` (do_sample=True, temperature=1.0, max_new_tokens=512), quality stays separate from performance — no RTF, no composite score. Env: gfx1151, torch 2.12.0+rocm7.14.0/HIP 7.14.60850, git HEAD `7cffe84…` | `.venv/bin/python scripts/quality_eval.py --manifest tests/data/quality_benchmark_manifest.json --out …` (sha256 `757314bc…`) |

### Errata — two labels inside `finetune-smoke-2026-09-20.txt` (transcript left unedited)

Per the no-hand-edit policy the transcript stays byte-for-byte as captured,
so the corrections live here instead. The underlying facts were verified in
[`docs/superpowers/reports/task-4-verification.md`](../docs/superpowers/reports/task-4-verification.md):

1. **Line 108** — `porcelain-exit=0 (empty output above = pristine)`. The
   `git status --porcelain` listing directly above (lines 71–107) is NOT
   empty: that invocation ran mid-bootstrap, after the reconstructed blobs
   had been staged but before the commit object was finalized, so it lists
   the 37 staged `A` files. The genuine pristine proofs are the later
   checks: `status --porcelain | wc -l` → `0` once HEAD was reconstructed
   (line ~194), the PHASE 4.5/4.6 closeout (`[status lines: 0]`, HEAD back
   at the pinned SHA), and the PHASE 4.10 final verification block
   (`wc -l` → 0, `diff --stat` → 0).
2. **Line 1546** — `as-is-run-exit=0`. That exit code belongs to the
   `… | tail -40` pipeline wrapper around the command, not to the training
   script: the AS-IS run itself FAILED, with the flash-attn `ImportError`
   quoted verbatim in the lines above the label (capturing that failure was
   the phase's stated purpose). The failed AS-IS run is real, intended
   evidence; the subsequent 12-step training ran only after the single
   disclosed `sdpa` line was applied inside the gitignored clone (and the
   clone was restored pristine afterwards).

### Errata — stale figure inside `vllm-vs-qwen-tts-2026-09-21.json` `fairness_notes` (JSON left unedited)

Per the no-hand-edit policy the machine-generated JSON stays byte-for-byte
as captured, so the correction lives here (found by the 2026-09-21 claims
audit, Task 16):

1. **`fairness_notes[0]`** — "longest output <=~20 s audio ~=<=240 tokens at
   12 Hz < 512". The stale figure understates the longest output in `rows`:
   the maximum audio duration on either side is **33.52 s** (vLLM side,
   prompt p4; qwen-side maximum 32.56 s), i.e. ≈ **402 tokens** at 12 Hz.
   The conclusion is unchanged — 402 < 512, so **no run on either side was
   truncated by any cap** and every run ended naturally at EOS — which is
   why `docs/vllm-omni-rocm.md`'s fairness note already carries the correct
   33.5 s / 402 token figures. Only the JSON's inline parenthetical was
   written from a pre-aggregation estimate.

## Reading guide

* Numbers quoted in the README / docs must trace to a file here. If you
  re-measure and get different results, cite your own artifact + date —
  RTFs drift with thermals and background load on unified memory.
* Historical transcripts may reference internal task numbers or pre-remote
  workflow states; those were accurate at recording time and are kept verbatim.
