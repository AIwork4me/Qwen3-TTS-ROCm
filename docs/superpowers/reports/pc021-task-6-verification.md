# TASK 6 — 0.6B BASE FINE-TUNING E2E — INDEPENDENT VERIFICATION REPORT (v0.2.1 production closure)

VERDICT: **PASS_WITH_CONCERNS** — every substantive gate reproduces (including an independent on-GPU reload of a produced checkpoint through the official API and a re-verification of the second upstream defect's root cause), but three minor documentation/provenance discrepancies were found, listed under DISCREPANCIES; none undermines the technical result.

Verifier: independent subagent, 2026-09-24. Repo HEAD during verification: 89b1ca166494a51ddabffd097bbe86292b3073fd (branch main, matches the task brief). Working tree clean except pre-existing untracked `.work-docker/` (present at session start, untouched by verifier). No tracked files modified; verifier writes were this report + `/tmp/task6-verify-reload.wav` scratch.

## CHECK 1 — Pristine-state gates — PASS

Pristine clone `.upstream/Qwen3-TTS`: `git rev-parse HEAD` → `022e286b98fbec7e1e916cb940cdf532cd9f488e` (matches pinned 022e286, upstream commit "fix finetuning bug"); `git status --porcelain | wc -l` → `0`; `git diff | wc -l` → `0`. Verified twice during this session (start of verification and after all GPU work) — the clone is pristine now, after the claimed runs.

Fix worktree `.upstream/Qwen3-TTS-fix`: branch `fix/finetuning-06b-text-projection`; `git log --oneline -3` → `512db9b fix(finetuning): project text embeddings before adding codec embeddings (0.6B: text_hidden 2048 -> hidden 1024); mirrors the inference path's mandatory talker.text_projection` / `48b8644 test(finetuning): cover default and explicit attention implementation` / `0be0026 fix(finetuning): make attention implementation configurable`; porcelain → `0`. Structure matches the claim exactly: fix commit on top of PR #373 head.

`git -C .upstream/Qwen3-TTS-fix diff 48b8644..512db9b` shows EXACTLY one hunk in `finetuning/sft_12hz.py` replacing `input_text_embedding = model.talker.model.text_embedding(input_text_ids) * text_embedding_mask` with `model.talker.text_projection(model.talker.model.text_embedding(input_text_ids))` plus a 5-line `LOCAL DISCLOSED WORKAROUND (v0.2.1 Task 6, 2026-09-24)` comment — nothing else in the diff. (The worktree's larger diff vs pinned 022e286 — sft_12hz.py 22 lines + tests/test_sft_attn_implementation.py 105 lines — is PR #373's own content on 0be0026/48b8644, correctly disclosed as workaround #1.)

All four paths confirmed gitignored via `git check-ignore`: `.upstream/Qwen3-TTS`, `.upstream/Qwen3-TTS-fix`, `.work-finetune/e2e06b-r1`, `.work-upstream/issue-draft-sft-06b-text-projection.md`.

## CHECK 2 — PR #373 live state — PASS

`gh api repos/QwenLM/Qwen3-TTS/pulls/373 --jq '{state,merged,head:.head.sha}'` → `{"head":"48b8644aac8512e6fdc0f4442788bf399c425fdb","merged":false,"state":"open"}`. OPEN / not merged / head 48b8644 unchanged — the fix worktree is still built on the current PR head. No change since the 2026-09-24 baseline.

## CHECK 3 — Root-cause validation (independent) — PASS

Configs read directly by verifier: `models/Qwen3-TTS-12Hz-0.6B-Base/config.json` → `talker_config.text_hidden_size=2048`, `talker_config.hidden_size=1024` (mismatch); `models/Qwen3-TTS-12Hz-1.7B-Base/config.json` → both `2048` (coincidental match, which is why 1.7B trains without the projection). Installed package `.venv/lib/python3.12/site-packages/qwen_tts/core/models/modeling_qwen3_tts.py`: line 1441 `self.codec_embedding = nn.Embedding(config.vocab_size, config.hidden_size)`; line 1442 `self.text_embedding = nn.Embedding(config.text_vocab_size, config.text_hidden_size)`; line 1978 `text_embed = self.talker.text_projection(self.talker.get_text_embeddings()(...))` — and the projection recurs at lines 2079/2124/2177/2201/2206/2230 (every inference path applies it). So the raw addition of text (2048-d) + codec (1024-d) embeddings in `sft_12hz.py:98` is a shape RuntimeError by construction on 0.6B, and the one-line workaround exactly mirrors inference semantics. Root cause CONFIRMED independently.

First-attempt log `/tmp/e2e06b-attempt1-upstream-bug.log` (46971 bytes, mtime 2026-09-24 21:02): header shows the fix worktree at that moment on branch `fix/finetuning-attn-implementation` HEAD `48b8644aac8512e6fdc0f4442788bf399c425fdb`, porcelain 0 — i.e., genuinely WITHOUT the text-projection commit; the run then fails with the verbatim traceback `File ".../Qwen3-TTS-fix/finetuning/sft_12hz.py", line 98, in train / input_embeddings = input_text_embedding + input_codec_embedding / RuntimeError: The size of tensor a (2048) must match the size of tensor b (1024) at non-singleton dimension 2` and summary markers `generator-exit=0 prepare-exit=0 sft-direct-exit=1` (plus launcher/reload=1 as downstream casualties), exactly as claimed. The narrative (fix commit 512db9b created after observing this failure) is internally consistent with the commit ordering.

## CHECK 4 — Checkpoint fingerprints — PASS

Verifier-run sha256sums: run1 phase-C `.work-finetune/e2e06b-r1/runs/checkpoint-epoch-1/model.safetensors` → `780b6e131a55103e2697b5173a42bed5b26b76a2d18f9c34c44f53b09d0ff858` (claimed 780b6e13…); run2 phase-C → `fd9d1aed64334f77662dfd17ddc5e74e815cf2f86cb1a8a701c45f4016965e0e` (claimed fd9d1aed…); run1 runs-mem epoch-1 → `5418b5866b11193c5f7080885233d0e8b601f0f708e6249cca3897bdf03b5c8c` (claimed 5418b586…); run2 runs-mem epoch-1 → `408c487ed30f3c033f8d0557e441fa8f2f8f3f070c85b4084f58b7873d1ab9bc` (claimed 408c487e…). All four match; all four differ from each other (independent trainings), and the datasets also differ across runs (`train_with_codes.jsonl` sha `ac4347f3…` run1 vs `f1d9e552…` run2, 12 lines each, all with `audio_codes`). Size 1,811,626,544 bytes ≈ 1.81 GB as claimed; `checkpoint-epoch-{0,1}` present in both `runs/` and `runs-mem/` of both runs.

## CHECK 5 — INDEPENDENT CHECKPOINT RELOAD (core gate) — PASS

Verifier ran, unmodified: `.venv/bin/python .work-finetune/e2e_reload_check.py --ckpt .work-finetune/e2e06b-r2/runs/checkpoint-epoch-1 --speaker e2e06b_r2_speaker --text 'The morning ferry crosses the bay in twenty minutes.' --out /tmp/task6-verify-reload.wav` → exit 0 with `[reload] loaded; supported speakers: ['e2e06b_r2_speaker']` / `generate_custom_voice took=4.6s sr=24000 n_wavs=1` / `WAV-SANE=True finite=True sr=24000 duration_s=2.56 peak=0.8320 rms=0.11666` / `RELOAD-AND-SYNTHESIS-OK`. Script audited first: it calls `qwen3_tts_rocm.loader.load`, which (src/qwen3_tts_rocm/loader.py line 299) calls `qwen_tts.Qwen3TTSModel.from_pretrained` verbatim — the official API, with the disclosed ROCm smart-default `attn_implementation='sdpa'`. Independent soundfile validation of the wav: sr=24000, 61440 frames (2.56 s), float64, peak 0.832, non-silent → `SOUNDFILE-INDEPENDENT-OK`. A freshly trained run-2 checkpoint therefore loads and synthesizes sane 24 kHz speech through the official path, reproduced by the verifier, not just by the implementer's transcript.

## CHECK 6 — Transcript consistency — PASS (with discrepancies D1–D3 below)

Both `/tmp/e2e06b-r1b-driver.log` (50715 B) and `/tmp/e2e06b-r2-driver.log` (50863 B) end `generator-exit=0 prepare-exit=0 sft-direct-exit=0 launcher-exit=0 reload-exit=0` and `=== TASK 6 E2E RUN {1,2} COMPLETE — … ===`, with epilogue `RUN1_RC=0 RUN2_RC=0`. Losses verbatim in-driver AND in the tee'd training logs `.work-finetune/e2e06b-r{1,2}.log` lines 22–23: run1 direct 12.5180→7.1567 (launcher rerun 15.3062→7.1528), run2 direct 13.0165→7.3493 (launcher rerun 13.0165→7.1318). Walls 19.5 s / 26.5 s. Peaks `[launcher] torch.cuda.max_memory_allocated: 8.649/8.650 GiB`, `max_memory_reserved: 9.064/9.072 GiB`. `CKPT-CONFIG-OK tts_model_type=custom_voice spk_id={'e2e06b_r{1,2}_speaker': 3000}` — independently confirmed by the verifier against the produced `checkpoint-epoch-1/config.json` (`tts_model_type=custom_voice` top-level; `talker_config.spk_id={'e2e06b_r2_speaker': 3000}`). Driver records downstream HEAD at run time `001389c4169fce7d54562522c9de46d54778934b`; verifier confirmed `001389c` is an ancestor of `89b1ca1` with exactly one commit between them touching only CHANGELOG/READMEs/docs/evidence (no code) — clean provenance. Driver-script pristine gates present before and after each run for the FIX worktree (porcelain 0, HEAD unchanged) plus a byte-diff proving `prepare_data.py` identical to the pristine clone. Evidence .txt files match the driver logs on every exit code, loss line, wall, peak, sha, and CKPT-CONFIG line quoted.

## CHECK 7 — Loss sanity / no overclaim — PASS

Each run executed 2 epochs × 6 optimizer steps = 12 steps (dataset 12 samples, batch 2; upstream steps per microbatch — same arithmetic as the validated 2026-09-20 smoke), with the per-epoch Step-0 print decreasing in every phase of both runs (12.52→7.16, 15.31→7.15, 13.02→7.35, 13.02→7.13). The docs treat this strictly as execution evidence: `docs/finetuning-rocm.md` 0.6B section states "**execution-validated only** — no convergence claim, no quality claim, no multi-speaker claim"; README/README_CN rows say "NO convergence / quality / multi-speaker claims" / "不做收敛/质量/多说话人声明"; CHANGELOG says "Execution-only: no convergence, quality, or multi-speaker claims." No convergence overclaim found anywhere.

## CHECK 8 — Docs audit at 89b1ca1 — PASS (with D2 affecting one index row's phrasing)

`docs/finetuning-rocm.md` "## 0.6B Base execution validation (v0.2.1 Task 6, 2026-09-24)": execution-only framing, both runs described (walls, peaks, fingerprints, reload OK), SECOND upstream defect disclosed with exact mechanism (2048≠1024, modeling `text_projection(get_text_embeddings()(...))` reference), issue "drafted but NOT filed (user-gated)", and the explicit pristine statement "The pristine pinned clone `.upstream/Qwen3-TTS` (022e286) is untouched — porcelain 0 before and after every run" (see D3 on the transcript provenance of that check). README.md row 70 and README_CN.md row 62: 1.7B·0.6B execution-only, no convergence/multi-speaker claims, both upstream blockers named, evidence links — verifier confirmed all linked evidence files exist (finetune-06b run1/run2, finetune-smoke-2026-09-20, upstream-372-root-cause, upstream-372-e2e-run1/2). `evidence/README.md` rows 94–95 present and substantively accurate (see D2). CHANGELOG `[Unreleased]` Task 6 entry accurate and disciplined. Issue draft exists locally at `.work-upstream/issue-draft-sft-06b-text-projection.md` (gitignored, 2730 B, mtime 21:23). NO upstream issue was actually filed: `gh api repos/QwenLM/Qwen3-TTS/issues?state=all&per_page=10` shows the newest items are #373 (PR, 2026-09-21), #372 (2026-09-20), #371 (2026-09-16)… — nothing from 2026-09-24, no 0.6B text-projection issue/PR. Drafted-not-filed confirmed.

## CHECK 9 — Zero-upstream-patch semantics — PASS

Pristine clone diff 0 lines (above). Installed `qwen_tts` in `.venv` never hand-edited: verifier recomputed the dist-info RECORD hashes for the whole package — 26 files checked, 0 mismatches, 0 missing; `pip check` → "No broken requirements found."; `grep -rln "LOCAL DISCLOSED WORKAROUND"` over `.venv/.../qwen_tts/` → no match; the workaround string `text_projection(model.talker.model.text_embedding` is absent from all tracked files of the downstream repo (`git grep` → no match). The SDPA workaround and the text-projection workaround exist ONLY inside the gitignored `.upstream/Qwen3-TTS-fix` worktree, exactly as disclosed.

## DISCREPANCIES (all minor, none technical)

D1 — Missing referenced transcript copies: the implementer's claim and, more importantly, the COMMITTED evidence file `evidence/finetune-06b-gfx1151-run1-2026-09-24.txt` line 2 point to a full transcript at `.work-finetune/e2e06b-r1-driver.log`; neither that file nor the claimed `.work-finetune/e2e06b-r2-driver.log` exists (`find` over `.work-finetune` confirms). The transcripts actually live only at `/tmp/e2e06b-r1b-driver.log` and `/tmp/e2e06b-r2-driver.log` (run2's evidence file correctly cites its `/tmp` path; run1's does not). The /tmp transcripts are complete and internally consistent, so this is a dangling pointer in committed evidence, not a fabrication.

D2 — Run-2 loss pairing across phases in summary surfaces: the evidence-index row 95 and the implementer's claim summary state run-2 "Loss 13.02→7.13", which pairs the direct-invocation first-step loss (13.0165) with the launcher-rerun second-step loss (7.1318); the direct run itself was 13.0165→7.3493 and the launcher rerun 13.0165→7.1318. Both numbers are real, and the run-2 evidence .txt records both phases correctly and clearly labeled — but the collapsed "13.02→7.13" mixes two different training invocations. Run1's summary (12.52→7.16) is phase-consistent.

D3 — Transcript attribution of the pristine-clone check: both evidence .txt files quote `$ git -C .upstream/Qwen3-TTS status --porcelain | wc -l  (pristine clone untouched)` → `0`, but that command appears nowhere in the driver script `.work-finetune/e2e06b-driver.sh` or in either driver transcript (the driver checks only the FIX worktree's porcelain, plus a prepare_data.py byte-diff against the pristine clone). The check is TRUE — the verifier independently confirms pristine HEAD 022e286, porcelain 0, diff 0 — but that quoted line was necessarily produced outside the driver run, so "porcelain 0 before and after every run" is attested by the implementer outside the transcript protocol rather than by the transcript itself.

## OBSERVATIONS

The verifier's reload synthesis produced a 2.56 s clip vs 2.08 s/1.68 s in the driver runs — expected sampling nondeterminism, irrelevant to the gate. Run-time repo state 001389c differs from the audited HEAD 89b1ca1 solely by the Task 6 evidence/docs commit itself. The second-upstream-defect finding is genuinely new relative to the 1.7B chain (1.7B's 2048/2048 coincidence masks it), is correctly root-caused against the installed modeling code, and the workaround is minimal and inference-faithful. Attempts, failures, and workarounds are disclosed rather than hidden, and the outward upstream action remains user-gated as required.

## FIX ROUND 1 — commit effa19e18efa651afc0f240cd96383833a439c0f (2026-09-24) — all three discrepancies resolved

Scope check: `git show --stat effa19e` shows EXACTLY the four claimed files and nothing else — `docs/finetuning-rocm.md` (+6/−2), `evidence/README.md` (+1/−1), `evidence/finetune-06b-gfx1151-run1-2026-09-24.txt` (+4/−0), `evidence/finetune-06b-gfx1151-run2-2026-09-24.txt` (+4/−0). A docs-only commit; no code, no checkpoints, no transcripts touched. Commit is HEAD with a clean tracked tree at re-verification time.

D1 — RESOLVED: the run-1 evidence .txt change is a PURE APPEND (`@@ -38,3 +38,7 @@`, zero deletions; every original line unchanged) adding a `=== CORRECTIONS (appended 2026-09-24 after Task 6 verifier findings — original capture above left unedited) ===` block stating the true transcript path `/tmp/e2e06b-r1b-driver.log` and that the header's `.work-finetune/e2e06b-r1-driver.log` pointer is wrong / nonexistent (run 2's pointer was correct) — exactly matching verifier finding D1. The wrong header line itself is deliberately left unedited with the correction appended below it, an acceptable errata pattern consistent with the repo's existing "Errata" convention in evidence/README.md.

D2 — RESOLVED: the run-2 evidence .txt correction is likewise a PURE APPEND (`@@ -33,3 +33,7 @@`, zero deletions) giving the phase-consistent pairs DIRECT 13.0165→7.3493 and launcher rerun 13.0165→7.1318, and stating plainly that any summary pairing 13.02 with 7.13 mixed the two invocations. Verifier re-checked both pairs against the source transcripts: direct run `.work-finetune/e2e06b-r2.log` lines 22–23 (12.5180-style printout: 13.0165, 7.3493) and `/tmp/e2e06b-r2-driver.log` lines 488–489 (13.0165, 7.1318) — numbers exact. The evidence index row (evidence/README.md line 95) now reads `direct-train Loss 13.02→7.35 (launcher rerun 13.02→7.13 — phase-consistent after verifier D2)` — phase-consistent and correctly rounded (7.3493→7.35), matching the fix-round scope verbatim.

D3 — RESOLVED: both evidence .txt correction blocks carry the attestation note (pristine-clone check run by the implementer OUTSIDE the driver protocol, which checks the fix worktree; independently re-verified by the Task 6 verifier after all GPU work), and `docs/finetuning-rocm.md` now states the same explicitly: "porcelain 0 verified by the implementer before and after the runs (outside the driver protocol, which checks the fix worktree) and independently re-verified by the Task 6 verifier after all GPU work." This is an accurate description of what the verifier actually did (pristine HEAD 022e286, porcelain 0, diff 0 confirmed twice, after all GPU work) and replaces the earlier over-attributed "porcelain 0 before and after every run" phrasing.

No new discrepancies introduced: the appended blocks' factual content (paths, losses, attestation status) was cross-checked line-by-line against the driver transcripts and verifier observations; nothing else in the four files changed semantics (the finetuning-rocm.md edit is confined to the pristine-attestation sentence; the index edit is confined to the run-2 loss phrase).

FINAL VERDICT (after fix round 1): **PASS** — all technical gates of TASK 6 were independently reproduced in round 0 (pristine-before/after, single-hunk disclosed workaround on PR-head 48b8644, live PR #373 OPEN, root cause 2048≠1024 confirmed in configs and installed modeling code, four checkpoint fingerprints matched with independent-train divergence, independent on-GPU reload of the run-2 checkpoint through the official API with sane 24 kHz non-silent synthesis, zero upstream/venv patches), and the three round-0 documentation discrepancies D1–D3 are now fully and accurately corrected in committed evidence at effa19e. No outstanding concerns.
