# Task 5 Verification — Long-Text + Soak Stability (v0.2.1, gfx1151)

Verdict: **PASS_WITH_CONCERNS** — every Task 5 acceptance gate passes under independent recomputation (zero failures across 815 resident requests + 10 recycle cycles, no memory-growth signal in my own regression/quantization analysis, ladder 8/8 with honest EOS-inference labeling and no maximum-length claim, scripts audited official-API-only with working nonzero-exit logic, GPU reruns green on both a 2-cycle recycle and a different model (0.6B) ladder), but three non-gate factual defects exist in curated evidence: (1) the ladder labels the long-tier token budget "official_default(2048)" when the actual effective official default on every local checkpoint is **8192** (verified live through the official `_merge_generate_kwargs`), making the recorded `audio_ceiling_seconds=170.67` for six rows arithmetically right on a wrong premise (should be 682.67) — hit_cap/natural-EOS verdicts are unaffected either way; (2) the soak transcript asserts "no latency drift trend", which my recompute contradicts (mean wall +14.6% block0→block9, exact RTF +10.0% first-half→second-half, present within both languages individually, not thermal, not longer outputs); (3) the attempt-1 soak narrative says "112 zh fails / 111 en OK in ~22 min" while the archived JSONL contains 114/113 over 475.5 s (~7.9 min) elapsed. None of these flip a gate; all are one-line evidence corrections. Repo state during verification: HEAD e84ca0f, clean tree (except pre-existing untracked `.work-docker/`), no tracked files modified by the verifier.

## Check 1a — Resident soak telemetry (my own analysis, no implementer code reused)

Script: `/tmp/task5_verify_analysis.py` (verifier-written, stdlib only) over `evidence/soak-gfx1151-2026-09-24-resident.jsonl`. Key verbatim output:

```
total_lines_records=817 meta=1 summary=1 request_records=815
summary: {"kind": "soak_summary", "stop_reason": "deadline", "total_requests": 815, "successes": 815, "failures": 0, "session_minutes": 60.04, "exit_code": 0}
ok_true=815 ok_false_or_missing=0 check successes+failures==count: 815+0=815 == 815? True
per-record successes+failures==request_index violations: 0; cumulative_failures!=failures violations: 0
RSS first10_avg=2.7700 last10_avg=2.7900 delta=+0.0200 GiB
WALL first10_avg=4.222 last10_avg=4.530 delta=+0.308s
  rss_gb: linreg slope=+1.939e-05/req (over 815 reqs => +0.0158 total) r2=0.7408 | firsthalf_mean=2.7806 secondhalf_mean=2.7900 delta=+0.0094 | min=2.770 max=2.790
  wall_seconds: linreg slope=+8.552e-04/req (over 815 reqs => +0.6970 total) r2=0.0910 | firsthalf_mean=4.1902 secondhalf_mean=4.5461 delta=+0.3559 | min=2.950 max=14.490
  peak_alloc_gb: linreg slope=-3.067e-06/req (over 815 reqs => -0.0025 total) r2=0.0028 | firsthalf_mean=4.0590 secondhalf_mean=4.0577 delta=-0.0013 | min=4.030 max=4.270
rocm.vram_used_gb samples=163 (expect ~every 5th of 815) min=1.080 max=1.120
temp_edge_C samples=163 min=60.0 max=65.0
sanity: rms<1e-3 violations=0 sr!=24000 violations=0 missing wall/audio=0
lang distribution: {'zh': 408, 'en': 407}
```

My verdict on monotonic memory growth: **no leak signal**. RSS is quantized to exactly three values — 2.77 (13 records), 2.78 (355), 2.79 (447) — with first occurrences at request 1, 14, and 369; i.e. two discrete +0.01 GiB (~10 MiB) steps early in the session, then perfectly flat at 2.79 for the final 446 consecutive requests (~33 min). The positive regression slope (+0.0158 GiB total, r2=0.74) is entirely an artifact of those two steps plus 0.01-GiB measurement resolution; there is no continuing growth in the last half of the run, `peak_alloc_gb` slope is ~zero with r2=0.003 (pure noise), and VRAM sits in a 1.08–1.12 GiB band. The implementer's "+0.02 GiB first-10→last-10, no monotonic growth observed" conclusion matches my numbers, though phrasing nit: the raw series is monotone *non-decreasing* (it never drops) — what it is not is *unbounded/continuing* growth, which is the leak-relevant property and is absent. The rtf field differs from `round(wall/audio,2)` on 60/815 records by exactly ±0.01 — benign rounding artifact (`rtf` is computed from full-precision wall/dur before the 2-decimal storage of wall/audio; deltas are symmetric +0.01×33 / −0.01×27). Wall-time is the one metric with a genuine drift; see Discrepancy 2.

## Check 1b — Recycle telemetry (my own analysis)

```
meta=1 summary=1 request_records=10
summary: {"kind": "soak_summary", "stop_reason": "cycles_completed", "total_requests": 10, "successes": 10, "failures": 0, "session_minutes": 1.23, "exit_code": 0}
all_ok=True cycles=[1..10]
rss_gb_after_unload sequence: [2.77, 2.9, 2.92, 2.92, 2.92, 2.92, 2.92, 2.92, 2.92, 2.92]
load_seconds: [4.76, 2.1, 2.11, 2.12, 2.04, 2.08, 2.1, 2.05, 2.09, 2.13]
first-cycle step=+0.130 GiB; tail (cycles 2+) spread=0.020 GiB — plateau interpretation: FAIR
```

10/10 cycles ok; RSS-after-unload steps +0.13 GiB after cycle 1 (allocator/interpreter steady-state) and then varies by ≤0.02 GiB across cycles 2–10. The implementer's plateau interpretation ("+0.15 GiB step then flat") is fair; my exact numbers are +0.13 step (2.77→2.90), one more +0.02 (→2.92 at cycle 3), then flat for 8 straight cycles. Warm load_seconds ~2.1 s vs 4.76 s first load is consistent allocator-cache behavior, and per-cycle peak_alloc (4.05–4.08) shows no cross-cycle GPU allocator growth.

## Check 1c — Per-request sanity fields and seed determinism

All 815 records carry `ok, wall_seconds, audio_seconds, rtf, rms, sr, peak_alloc_gb, peak_reserved_gb, request_index, lang, elapsed_seconds, successes, failures, cumulative_failures, rss_gb, rocm`; every record has rms≥0.0903≥1e-3, sr=24000, positive wall/audio (recomputed violations: 0). Seeds are not stored per-record (minor observation) but are fully determined by `meta.seed_base=20260925` + request_index (script: `seed = args.seed_base + i`, request_index=i+1, so request k uses 20260925+k−1 — matches the required 20260925+i scheme). I confirmed determinism *empirically*: my recycle rerun (same seed_base, indices 1–2) reproduced the archived records byte-for-byte on the deterministic fields — `audio_seconds` 3.04/3.36 and `rms` 0.161/0.0903 identical to archived resident records #1/#2. Temperature is kept as the raw rocm-smi block (parse-averse, honest) alongside a parsed `vram_used_gb`.

## Check 2 — Ladder JSON (`evidence/long-text-gfx1151-2026-09-24.json`)

```
rows=8 all_ok=True
zh: chars=[17, 53, 121, 245] strictly_increasing=True tiers=['short','medium','long','very-long']
en: chars=[60, 179, 453, 892] strictly_increasing=True
  zh/short: budget=512 ceiling=42.67 (512/12=42.67 match=True) hit_cap=False rtf=1.81 exact=1.8125 match=True peak=4.06
  zh/medium: budget=512 ceiling=42.67 match=True hit_cap=False rtf=1.23 match=True peak=4.30
  zh/long: budget='official_default(2048)' ceiling=170.67 (2048/12=170.67 match=True) hit_cap=False rtf=1.28 match=True peak=4.66
  zh/very-long: budget='official_default(2048)' ceiling=170.67 match=True hit_cap=False rtf=1.34 match=True peak=4.72 audio=58.16
  en/short: 512/1.29/4.07; en/medium: 512/1.23/4.30; en/long: 2048-default/1.26/4.66; en/very-long: 2048-default/1.32/4.72 audio=57.84
rtf range: 1.23–1.81; peak range: 4.06–4.72; rss range: 2.77–2.85
```

Everything checks: 8/8 ok, chars strictly increasing per lang with the exact claimed values, budget pattern 512/512/default/default, ceiling arithmetic internally consistent, hit_cap=False on all rows, every rtf equals round(wall/audio,2), peak 4.06→4.72 and rss 2.77–2.85 match claims, very-long audio 57.84/58.16 s matches "57.8–58.2 s natural EOS", meta carries the explicit truncation_note disclaiming token-count observability, and the medium→very-long RTF band is 1.23–1.34 (short 1.29/1.81) exactly as claimed. **However** the "official_default(2048)" value itself is wrong — see Discrepancy 1. Note the very-long durations (~58 s ≈ 700 tokens at 12 Hz > 512) independently prove the long tiers really did run past the 512 guardrail under the checkpoint default, so the *mechanism* label (kwarg omitted → official default) is correct; only the numeric value is misidentified.

## Check 3 — Script audits

`scripts/soak_test.py`: official-API-only — drives `loader.load()` (docstring-verified to return the native `Qwen3TTSModel.from_pretrained` object, never a wrapper) and only `generate_custom_voice`/`generate_voice_design`/`generate_voice_clone` with official kwargs; deterministic 6-sentence zh/en manifest with `seed_base + i`; per-request JSONL with flush; correctness gate (finite, RMS≥1e-3, 24 kHz, 0.5–60 s) records verbatim tracebacks and the summary writes `exit_code: 0 if failures == 0 else 1` with `main()` returning 1 and printing SOAK-FAILED — nonzero-exit logic present and correct; recycle mode records per-cycle `load_seconds` and `rss_gb_after_unload` after `loader.unload`; docstring explicitly states NO leak-free claim is made. The LANG_NAMES fix is present (line 58) with the archived-bug comment naming "soak attempt 1 archived the same KeyError('zh') bug class as the ladder's first attempt — every zh request failed while every en request passed (2026-09-24)". `scripts/longtext_ladder.py`: same official-API discipline plus `testing.assert_wav_sane`; fixed MANIFEST with recorded char counts; budget policy short/medium=512 explicit, long tiers kwarg-omitted; hit_cap threshold dur ≥ 0.90×ceiling with the inference disclosed in code, docstring, and output meta; nonzero exit on any tier failure (verified live in the attempt-1 archive, `LADDER_RC=1`); its LANG_NAMES comment references the archived attempt-1 KeyError evidence. Both bug-class comments reference the archived attempts as required.

## Check 4 — GPU spot reruns (both green)

Recycle on 1.7B `custom-voice` at HEAD e84ca0f: `cycle#1 OK load=4.65s wall=5.65s rss_after_unload=2.77GiB` / `cycle#2 OK load=1.94s wall=4.1s rss_after_unload=2.89GiB` / `SOAK-OK` / rc=0 — and the rerun's records 1–2 reproduce the archived run's audio/rms byte-identically (determinism confirmed). Ladder on `custom-voice-0.6b` (different model from the archived 1.7B run): all 8 tiers OK, e.g. `zh/very-long chars=245 wall=60.83s audio=54.32s rtf=1.12 budget=official_default(2048) hit_cap=False peak=2.83GiB` and `en/very-long chars=892 wall=64.4s audio=58.32s rtf=1.1 hit_cap=False`, `LONGTEXT-LADDER-OK`, rc=0 — structural success on a second model, natural EOS again (54/58 s audio far under any ceiling), JSON at `/tmp/task5-verify-ladder.json`. Both reruns' outputs live in `/tmp/task5-verify-recycle.jsonl` and `/tmp/task5-verify-ladder.json`.

## Check 5 — Transcript accuracy vs artifacts

`evidence/soak-gfx1151-2026-09-24.txt`: the 815/60.04/815/815/zero-failures summary line matches the JSONL verbatim; the resident analysis numbers (+0.02 RSS, peak 4.058→4.057, VRAM 1.08–1.12, wall range 2.95–14.49, audio 2.32–10.48 — all recomputed by me and confirmed) match; the recycle block matches the recycle JSONL record-for-record; the "NO 'memory leak free' claim" caveat is present. **One line fails my recompute**: "Per-request wall 2.95-14.49 s (audio 2.32-10.48 s) — no latency drift trend (full series in JSONL)" — see Discrepancy 2. `evidence/long-text-gfx1151-2026-09-24.txt`: result table matches the JSON cell-for-cell; observations 1–4 (RTF bands, no tier hit budget / 57.8–58.2 s natural EOS, peak 4.06→4.72, RSS 2.77–2.85, scope-limited to 1.7B CustomVoice, no maximum-length claim) all verified against the JSON; line 3's "checkpoint default 2048" carries Discrepancy 1. Neither transcript nor any doc makes a leak-free or maximum-length claim (grepped).

## Check 6 — Docs audit at e84ca0f

Task-5 commits (f4a46a7, e125588, bda6b3b, e84ca0f) touched only `scripts/longtext_ladder.py`, `scripts/soak_test.py`, the four evidence artifacts, `README.md` (+1), `README_CN.md` (+1), `evidence/README.md` (+2), `CHANGELOG.md` (+14) — historical docs untouched. `README.md:67` capability row: "ladder 8/8 tiers to 892 chars / 58 s audio (natural EOS, no max-length claim); 60-min resident soak 815/815 OK, zero failures; 10/10 load→generate→unload cycles (RSS plateau; no leak-free claim)" — every number artifact-backed, both disclaimers present, no overclaim. `README_CN.md:59` carries the same row in Chinese with 不声明最大长度/不做无泄漏声明 — equivalent. `CHANGELOG.md` Task 5 entry (lines 347–360) is accurate and scoped, mentions no 2048 value, archives both script-bug iterations. `evidence/README.md:92-93` rows match artifacts in every number I checked, except line 92 repeats "long tiers ran the official default 2048" (Discrepancy 1). The pre-existing `README.md:315` "upstream 2048 default" text predates Task 5 (not in its diffs).

## Check 7 — Attempt-1 archives

All four `/tmp` archives exist. `/tmp/longtext-attempt1-failed.log`: exactly 4 `[longtext] FAIL zh/<tier>` blocks each with `Traceback` → `KeyError: 'zh'`, ending `LADDER_RC=1`; `.json` confirms all 4 zh tiers ok=false error `KeyError: 'zh'`, all 4 en tiers ok=true — matches the claim (claimed commit 46070b6 exists in reflog with parent c2e422e; the archived JSON's meta.git_head says c2e422e, i.e. the run executed at the c2e422e working tree — trivial provenance nuance, content identical to 46070b6's buggy script). `/tmp/soak-attempt1-failed.log` shows the exact alternating pattern (`req#1 FAIL ... req#2 OK ... req#3 FAIL`), and `/tmp/soak-attempt1-failed-resident.jsonl` contains 227 request records: **114 zh failures (all `KeyError: 'zh'`) vs 113 en OK**, last record `elapsed_seconds: 475.5`, no summary record (run stopped mid-session) — the bug-class claim is confirmed, but the claimed counts "112 vs 111 in ~22 min" are off: archive says 114/113 in ~7.9 min of session elapsed (see Discrepancy 3).

## Discrepancies

1. **Wrong official-default value (2048 vs 8192) in ladder budget labels and ceilings.** All five local checkpoints' `generation_config.json` set `"max_new_tokens": 8192`, and the official package resolves defaults as user-value → `generate_defaults` (from `model.generate_config`) → hard default 2048; I loaded the 1.7B CustomVoice and called the official `_merge_generate_kwargs()` verbatim: `RESOLVED default max_new_tokens via official _merge_generate_kwargs: 8192`. Therefore the ladder's `token_budget: "official_default(2048)"` and `audio_ceiling_seconds: 170.67` on the six long-tier rows (JSON + transcript table + script docstring "the checkpoint's generate_config.json default of 2048 applies" + `evidence/README.md` line 92) are factually wrong; the correct ceiling at 12 Hz is 8192/12 = 682.67 s. Impact on conclusions: none — hit_cap=False / natural_eos_presumed=True hold under either ceiling (observed durations 28.56–58.16 s are far below both the 153.6 s and 614.4 s thresholds), no maximum-length claim was made, and the very-long audio (~58 s ≈ 700 tokens) still proves the guardrail was genuinely bypassed. It also understates the worst-case degenerate-run bound (8192 tokens ≈ 4× the "≲23 min" figure the docstring derives from a presumed 2048 cap). The 2048 belief originates in dangling commit 46070b6 ("official 2048 default budget") and in pre-existing `voice_workflow.py` docs — but Task 5's new artifacts propagate it. Fix: relabel to `official_default(8192)` / ceiling 682.67 in script, JSON meta note, transcript, and evidence-index row (numbers that matter — durations, RTF, memory — are unaffected).
2. **"no latency drift trend" (soak transcript line 21) is contradicted by my recompute.** Ten-block wall means rise monotonically 3.996, 4.112, 4.196, 4.236, 4.415, 4.415, 4.517, 4.583, 4.625, 4.582 s (+14.6% first→last block; halves 4.190→4.546, medians 4.150→4.505); exact RTF rises 1.296→1.425 (+10.0%) while audio duration *falls* slightly (3.235→3.190 s), so it is genuine per-token slowdown, present within each language separately (zh within-lang slope +1.5 ms/req, en +1.9 ms/req, so not manifest-mix), and NOT thermal (temp quartile means 64.4→63.0→62.6→62.6 °C, Pearson r(temp, wall) = −0.221). No memory metric moves with it and zero requests failed, so this is not a Task 5 gate failure (gates are memory growth + failure rate), but the curated transcript asserts something the data does not support. Fix: replace with the measured drift statement (e.g. "mean wall drifted ~+10–15% over the hour with flat audio, flat memory, zero failures; cause not identified, not thermal").
3. **Attempt-1 soak narrative counts and duration don't match the archive.** Claim (transcript line 5, commit e125588 message): "112 zh requests failed ... 111 en requests passed", "~22 min". Archive: 114 zh failures / 113 en OK, last `elapsed_seconds 475.5` (~7.9 min of session; the missing zh requests fail in milliseconds, so 113 en × ~4.2 s ≈ 475 s is internally consistent). Bug-class and stack-stability conclusions unaffected. Fix: correct to 114/113 and ~8 min (or "stopped at request 227").
4. Minor, no action strictly needed: per-request seeds are not stored in the JSONL (derivable from meta.seed_base + request_index; determinism independently confirmed); "no monotonic growth observed" phrasing is loose since the quantized RSS series is technically monotone non-decreasing (two steps) — "no continuing/unbounded growth" would be precise; ladder attempt-1 run executed at tree c2e422e though attributed to commit 46070b6 (same content, reflog-resident).

## Observations

The single wall outlier (14.49 s at request 369) coincides exactly with the second RSS step (2.78→2.79), suggesting one transient host hiccup, fully recovered, no failures. RTF field's ±0.01 rounding deltas (60/815) are symmetric and benign. My recycle rerun reproduced archived records byte-identically on seeded fields — strong evidence the deterministic-manifest requirement is genuinely met, not just coded. Recycle plateau interpretation is fair (+0.13 GiB first-cycle step, ≤0.02 GiB spread across cycles 2–10). The 0.6B ladder rerun green on first attempt with natural EOS on all tiers extends the ladder's structural result to a second model (outside the implementer's claimed 1.7B-only scope — a bonus, not a requirement). Docs hygiene is otherwise excellent: capability rows scoped to exactly what was measured, both honesty disclaimers (no max-length, no leak-free) present in both languages, CHANGELOG accurate, historical docs untouched.

Verdict restated: **PASS_WITH_CONCERNS** — gates all pass independently; recommend the three one-line evidence corrections (2048→8192 relabel, latency-drift line, attempt-1 counts/duration) before release closure. Reruns and analysis scripts: `/tmp/task5-verify-recycle.jsonl`, `/tmp/task5-verify-ladder.json`, `/tmp/task5_verify_analysis.py`.

## Fix round 1 re-verification

Scope: independent re-verification of the fix round for findings D1–D3 only (commits 44d6f0b script fix + 4b08330 evidence/doc rerun, HEAD 4b08330), assuming the implementer is wrong until proven otherwise. No tracked files modified; the only write is this report append. Repo state: HEAD 4b08330, tracked tree clean (untracked: pre-existing `.work-docker/`, this report).

**Check F1 — commit scoping.** `git log --oneline 4b08330~3..4b08330` shows e84ca0f → 44d6f0b → 4b08330. `git show --stat 44d6f0b`: touches ONLY `scripts/longtext_ladder.py` (+29/−9); message starts "fix: ladder long-tier budget resolved" as claimed. `git show --stat 4b08330`: touches ONLY `evidence/README.md`, `evidence/long-text-gfx1151-2026-09-24.json`, `evidence/long-text-gfx1151-2026-09-24.txt`, `evidence/soak-gfx1151-2026-09-24.txt` — no collateral files, historical docs untouched. The script fix is substantive, not cosmetic: new `_effective_default_budget(model)` reads `model.model.generation_config.max_new_tokens` (observation only, nothing modified) at run time, `_run_tier` resolves the long-tier budget through it, the ceiling is computed from the resolved integer (`ceiling = budget / HZ`), the row records `token_budget` as a plain integer, and the module docstring now states 8192 ≈ 682.7 s at 12 Hz with an explicit note that attempt 2 wrongly assumed the training-side 2048. PASS.

**Check F2 — new ladder JSON (strict programmatic parse).** 8 rows, all `ok=true`; zh chars exactly 17/53/121/245 and en exactly 60/179/453/892 (also matching `meta.manifest_chars`); short+medium rows `token_budget=512` (int) / `audio_ceiling_seconds=42.67` (=512/12); the FOUR long-tier rows have `token_budget` of Python type `int` equal to **8192** (not a string) and `audio_ceiling_seconds == 682.67` (=8192/12, recomputed: 8192/12 = 682.666… → 682.67 exact to 2 dp); `hit_cap=false` on all 8; every `rtf` equals `round(wall_seconds/audio_seconds, 2)` recomputed exactly (all 8 rows, zero mismatches); wall 4.31–75.91 s, audio 3.2–58.16 s, peak_alloc 4.06–4.72 GiB, RSS 2.77–2.84 GiB — all plausible and consistent with renders ~28–58 s on long tiers; `meta.git_head=44d6f0b` and timestamp 20:42+ local confirm a genuine post-fix rerun, not an edit of the old artifact. PASS.

**Check F3 — .txt table vs JSON + attempt history.** Parsed all 8 markdown table rows cell-by-cell against the JSON: chars, wall, audio, RTF, budget, hit_cap, peak, rss match on every row, every column (0 mismatches). The "HONEST ITERATION HISTORY" section accurately describes attempt 1 (46070b6, all 4 zh tiers `KeyError('zh')`, exit 1, `/tmp/longtext-attempt1-failed.{log,json}`), attempt 2 (f4a46a7, 8/8 green but budget label wrongly assumed 2048 / ceiling 170.67, "verifier D1 proved the shipped checkpoints' generation default is 8192 (ceiling 682.67); hit_cap verdicts unaffected (renders 28–58 s)", archived at `/tmp/longtext-attempt2-wrongceiling.{txt,json}`), attempt 3 (this run, correct ceilings). Corroborated the rerun is real: `/tmp/longtext-run3.log` (mtime 20:47:21) contains the identical stdout table plus `LONGTEXT-LADDER-OK` / `LADDER3_RC=0`. PASS.

**Check F4 — soak CORRECTIONS block.** `git diff e84ca0f..4b08330 -- evidence/soak-gfx1151-2026-09-24.txt` shows ONLY 4 appended lines (0 deletions) — the original capture above the block is byte-identical to e84ca0f. The block quotes round-1's numbers verbatim: D2 wall 3.996→4.582 s (+14.6%), RTF 1.296→1.425 (+10.0%), audio slightly falling, temperature falling 64.4→62.6 °C (not thermal), zero failures, "cause uncharacterized"; D3 114 zh / 113 en over 475.5 s (~7.9 min) with the old "112/111 in ~22 min" explicitly marked as mid-run grep counts. Independent recompute from the raw JSONL confirms the substance: my 10-block wall means are 3.996, 4.112, 4.196, 4.236, 4.415, 4.415, 4.517, 4.583, 4.625, 4.576 — the first nine digits match round-1's series exactly and first→last is +14.5% vs round-1's +14.6% (0.1 pp block-boundary variance, last block 4.576 vs 4.582); audio block means fall 3.261→3.185; failures 0/815; last rocm-smi sample in the JSONL reads 62.0 °C (edge) — flat-to-falling, "not thermal" holds; 60.03 session minutes. D3 reproduced EXACTLY: `/tmp/soak-attempt1-failed-resident.jsonl` contains 114 zh rows all failed with error class `KeyError: 'zh'` and 113 en rows all ok, max `elapsed_seconds` = 475.5 = 7.92 min. Minor note (not a defect in the fix): round-1's exact-RTF endpoints (1.296→1.425, +10.0%) are method-sensitive — plain 10-block means of per-request wall/audio give 1.242→1.445 (+16.3%) — but the corrections block faithfully quotes the round-1 figures it was required to adopt, and every method confirms a genuine ~+10–16% RTF / ~+15% wall upward drift with flat audio and zero failures, so the "~15%" README wording is honest and, for RTF, conservative. PASS.

**Check F5 — evidence/README.md rows.** `git diff e84ca0f..4b08330 -- evidence/README.md` changes ONLY the two Task 5 rows (line 92 ladder, line 93 soak), no other rows. Ladder row now states the long tiers ran "the checkpoint's own generation default — **8192** tokens (resolved from the official generation config, ceiling 682.7 s; ladder attempt 2 wrongly assumed the 2048 training-side default, archived)". Soak row now states "Per-request latency drifted up ~15% across the hour (block-mean wall 4.00→4.58 s, RTF +10%, temperature falling — not thermal; zero failures, cause uncharacterized)" and "Attempt 1 archived (7.9 min): 114 zh `KeyError('zh')` script bugs vs 113 en OK". All required tokens present: 8192, 682.7 s, attempt-2 archive note, ~15%, 114 zh, 113 en, 7.9 min. PASS.

**Check F6 — /tmp archives.** `/tmp/longtext-attempt2-wrongceiling.{json,txt}` exist (19:11/19:12); the archived JSON contains exactly 4 rows with `"token_budget": "official_default(2048)"` (string) and ceilings 4×170.67 + 4×42.67 — the wrong-premise artifact preserved as claimed; `/tmp/longtext-attempt1-failed.{log,json}` also present. PASS.

**Fix-round verdict: PASS** — all three concerns (D1, D2, D3) are correctly and honestly remediated: the wrong 2048 premise is fixed at the script level (runtime resolution from the checkpoint generation config, observation-only) and the ladder was genuinely RERUN producing a clean artifact with the correct 8192/682.67 labels and an accurate attempt history; the soak drift contradiction and the attempt-1 miscounts are corrected by an appended erratum with the original capture byte-identical, and the corrected numbers are independently reproduced from the raw JSONL (D3 exactly; D2 to within 0.1 pp block-boundary variance on wall and with the RTF-magnitude caveat noted above, which only makes the documented drift more conservative, not less); the evidence index rows are trued with no collateral edits. No new discrepancies found.

**Task 5 overall verdict (fix round 1): PASS** — the round-1 gates all passed independently and the only blockers to a clean PASS (D1–D3 evidence-integrity defects) are now verified fixed at HEAD 4b08330; the one residual nuance (round-1's exact-RTF +10.0% endpoint is method-sensitive vs my +16.3% recomputation) is recorded here for the record and does not affect any gate, claim, or the direction/magnitude honesty of the corrected evidence.
