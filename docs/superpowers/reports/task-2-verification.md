# Task 2 independent verification (verifier run, 2026-09-20 ~21:40–21:55 +08:00)

## VERIFICATION REPORT — TASK 2
### Verdict
**PASS**

### Acceptance criteria
* [x] **1 — All 10 official languages via CustomVoice on the real GPU.** JSON `results.custom_voice` has exactly 10 rows whose `language_id`s are `{chinese, english, japanese, korean, german, french, russian, portuguese, spanish, italian}`; my own independent live probe (`loader.load("custom-voice")` → `get_supported_languages()`) returned `['auto','chinese','english','french','german','italian','japanese','korean','portuguese','russian','spanish']` — identical (count + identities) to the JSON `supported_languages_runtime` and to ground-truth §5a; minus `auto` = the 10 languages, exactly covered. Drift is hard-gated in `main()` (exit 2, both directions), so the passing run mechanically proves coverage.
* [x] **2 — Machine-readable evidence, internally consistent.** `evidence/multilingual-matrix.json`: full env header (timestamp, `AMD Radeon 8060S Graphics`, `gfx1151`, torch `2.12.0+rocm7.14.0`, hip `7.14.60850`, qwen-tts `0.1.1`, git HEAD `fb8bc60…`, upstream SHA `022e286b…`, `max_new_tokens: 512`, model alias per section: custom-voice / custom-voice / voice-design / base); every row carries `language, language_id, wall_s, audio_s, checks{finite, non_silent, valid_sr, bounded_duration, rms, duration_s}, pass, error`. Programmatic consistency vs the `.txt` transcript passed for **both** the archived (staged) blobs and my regenerated artifacts: same timestamp line, same summary line (24/24 + 4/4 + wall total), all 28 `[cv]/[vd]/[cv-ref]/[clone]` lines match JSON rows cell-for-cell in order (language, id, wall, pass, audio).
* [x] **3 — No pronunciation-quality overclaim in any file in the diff.** Greps over README.md, README_CN.md, docs/ and the full diff for pronun*/native/fluent/accent/intelligible/human-like/发音/口音/自然度/流畅 etc. return only the *disclaimers* ("This is NOT a pronunciation-quality claim" / "这不是发音质量声明" + representative-not-exhaustive wording). The only capability claim anywhere is "end-to-end generation completed on Radeon" sourced from waveform sanity.
* [x] **4 — Exactly the 4 brief pairs, transcript rule verifiably enforced.** JSON `clone_pairs` = {(en→zh), (zh→en), (ja→en), (fr→zh)} and the 4 `cross_lingual_clone` rows match those pairs. Byte-exact assertion `row["ref_text"] == manifest sentence for row["ref_language"]` passed for all 4 rows in **both** runs (e.g. `The blue kite rises slowly over the quiet harbor.`, `秋风吹过湖面，带来一丝清凉的气息。`, `春の朝、小鳥たちが窓の外で楽しそうに鳴いています。`, `La lumière du matin traverse les grandes fenêtres.`). The rule is structural in the code (same `Sample` object feeds `generate_custom_voice` and `create_voice_clone_prompt(ref_audio=(wav, sr), ref_text=…)`).
* [x] **5 — Documentation traceable to evidence.** README.md "Multilingual capability matrix" (10 rows) and README_CN "多语言能力矩阵" (10 bilingual rows) verified row-by-row against the JSON `summary.per_language` (zh: ref en, fr; en: ref zh, ja; other 8: —). The EN caption is the brief's caption verbatim modulo line wrapping; the CN mirror carries all three required elements (waveform-sanity definition + see evidence JSON + NOT-a-pronunciation-claim + representative-4-pairs). `evidence/README.md` gained index rows for both artifacts; CHANGELOG entry sits under `## [0.2.0] - 2026-09-20` → `### Added` (one minor inaccuracy in the txt row, see Problems #1).
* [x] **6 — Prior suites still green.** Full GPU suite `-m gpu -q`: **34 passed**, exit 0 (285 s). Full CPU suite `-m 'not gpu' -q`: **229 passed** (216 prior + 13 new), 34 deselected, exit 0. `tests/test_official_demo_parity.py -q`: 3 passed, exit 0.
* [x] **7 — No upstream patches.** Staged diff touches exactly 9 repo files (no site-packages, no vendored source); `git status` shows nothing else. pip RECORD of installed `qwen-tts 0.1.1` hash-verified: 26 files hash-checked, 0 mismatches/missing → RECORD-INTACT.

### Code reviewed
Full staged diff `review-task2-fb8bc60-staged.diff` (5 278 lines), line by line:
- `scripts/validate_languages.py` (1007 lines) — stdlib-only import (verified: the 13 tests import it in 0.04 s); pure helpers `load_manifest` / `resolve_language` / `check_coverage` / `evaluate_waveform` / `summarize_results`; GPU drivers `run_custom_voice_matrix`, `synthesize_clone_references`, `run_voice_design_matrix`, `run_cross_lingual_clone`; `main()` one-model-resident-at-a-time with `loader.unload` between phases, drift gate both directions (exit 2), per-cell failures recorded as failed rows, exit 0 iff all cells pass. All generation calls keyword-first with `max_new_tokens` appended on **every** generation; `create_voice_clone_prompt` correctly receives `(wav, sr)` tuple + `ref_text` per the official signature (verified against installed `Qwen3TTSModel` signatures: `generate_custom_voice(text, speaker, language, instruct, …)`, `generate_voice_design(text, instruct, language, …)`, `generate_voice_clone(text, language, …, voice_clone_prompt, …)`, `create_voice_clone_prompt(ref_audio, ref_text, …)`). `evaluate_waveform` = finite/non-silent(rms>1e-3)/sr>0/0<d≤60 s.
- `tests/test_validate_languages.py` (246 lines, 13 tests, no gpu marker) — real-fixture consistency; manifest rejection cases; drift KeyError naming both sides; coverage; waveform gate incl. NaN/silent/invalid-sr/over-long/empty; aggregation shape.
- `tests/data/multilingual_samples.json` — verbatim from the brief (10 sentences, 10 descriptions, 4 pairs).
- README.md / README_CN.md / CHANGELOG.md / evidence/README.md sections as above.

### Tests executed
Exact commands (cwd `/home/amd/Desktop/Qwen3-TTS-ROCm`) and exit codes:
1. `.venv/bin/python -m pytest -m 'not gpu' -q` → `229 passed, 34 deselected in 7.11s` — **exit 0**
2. `.venv/bin/python -m pytest tests/test_validate_languages.py -q` → `13 passed in 0.04s` — **exit 0**
3. `.venv/bin/python -m pytest -m gpu -q` → `34 passed, 229 deselected in 285.22s` — **exit 0**
4. `.venv/bin/python scripts/validate_languages.py 2>&1 | tee evidence/multilingual-matrix.txt` (tee's stdout suppressed to keep the audit log readable; file content unaffected) → script **exit 0** (`PIPESTATUS[0]=0`), tee exit 0
5. `.venv/bin/python -m pytest tests/test_official_demo_parity.py -q` → `3 passed in 12.73s` — **exit 0**
6. JSON parse + schema/consistency assertion script (json.load + asserts on meta fields, per-section aliases, 4×N row schema, pass/error, language_id membership, clone pairs, ref_text == manifest, summary recount, txt↔JSON cell-by-cell match) → **exit 0**, run twice: against the regenerated working-tree artifacts AND against the staged (archived) blobs extracted via `git show :evidence/…`
7. Independent GPU probe `loader.load("custom-voice")` → `get_supported_languages()` / `get_supported_speakers()` (speaker[0]=`aiden`, matches transcript) → **exit 0**
8. qwen-tts pip RECORD hash verification (26 files) → **exit 0**, RECORD-INTACT
9. `.venv/bin/ruff check scripts/validate_languages.py tests/test_validate_languages.py` → All checks passed — **exit 0**

### Runtime evidence
Authorized regeneration (binds evidence to the exact committed script, resolving implementer concern #4): command 4 above on the real gfx1151 GPU — `RESULT: PASS`, `[summary] matrix cells: 24/24 passed; clone-reference generations: 4/4 passed; wall total 152.6s` (archived run: 147.5 s; only timings differ, structure and counts identical — JSON diff vs staged blob is 118 value-only line changes). Transcript header complete: `timestamp=2026-09-20T21:46:57+08:00`, `gpu=AMD Radeon 8060S Graphics gfx=gfx1151`, `torch=2.12.0+rocm7.14.0 hip=7.14.60850 qwen-tts=0.1.1`, `git_head=fb8bc60…`, `upstream_qwen3_tts_sha=022e286b…`, manifest path, `max_new_tokens=512 (every generation)`. 28 `pass=True` cell lines, 0 `pass=False`. Section model aliases: custom-voice (load 4.5 s) → voice-design (2.0 s) → base (1.9 s), one resident at a time.

### Claims audit
Adversarial greps (EN+CN) over every file in the diff plus all of README.md / README_CN.md / docs/: the only occurrences of pronunciation-adjacent vocabulary are the mandated disclaimers. No "sounds natural / fluent / native / accent / 音质 / 口音" claims. Clone coverage is stated as "representative (4 pairs), not exhaustive" in README, README_CN, CHANGELOG, evidence/README.md, the JSON `meta.claim` and the transcript caption. The only capability claim is "end-to-end generation completed on Radeon" derived from waveform sanity, exactly as the brief's caption requires (verified verbatim modulo line wrapping). Minor claim-hygiene items are listed under Problems (#1, #3) — neither is a capability overclaim.

### Regression audit
CPU 229/229, GPU 34/34, demo-parity 3/3, ruff clean. No prior test modified or loosened: the diff adds files only, except the four docs; no test-file edits outside the new `tests/test_validate_languages.py`. The archived evidence and the regenerated evidence agree on all counts and verdicts. Working tree after my audit: only `evidence/multilingual-matrix.json` / `.txt` modified (unstaged) — that is my authorized regeneration; the staged blobs are unchanged (json `739b4d3`, txt `fdb0b61`, matching the review diff).

### Problems found
1. **evidence/README.md txt-index row over-describes the artifact**: it says the transcript contains "`RESULT: PASS`, `RUN_EXIT=0`" — `RUN_EXIT=0` appears in *neither* the archived nor the regenerated transcript (it was printed by the implementer's shell wrapper outside the `tee`). Cosmetic inaccuracy in a diff'd file; the exit-0 fact itself is independently confirmed by my regeneration.
2. **Staged evidence is the pre-ruff-cleanup run** (implementer deviation #4). My regeneration from the committed script passed 24/24, exit 0, and now sits **unstaged** in the working tree (`AM` status). Before committing, the orchestrator must re-`git add evidence/multilingual-matrix.json evidence/multilingual-matrix.txt` (Step 2.9's `git add -A` covers this) so the committed evidence is the run produced by the committed script. Regenerated artifacts differ only in timestamps/timings and the `[evidence] wrote …` path being cwd-relative.
3. **Stale suite counts in unchanged README/README_CN lines** ("passes 215 CPU tests", "216 CPU + 34 GPU = 250 / 250"): after Task 2's 13 new CPU tests the real numbers are 228 CI CPU (+1 HIP-gated) / 229 host CPU + 34 GPU = 263. Understatement, not overclaim; the lines are unchanged context inside a diff'd file and no acceptance criterion covers them, but they are now inaccurate.
4. **`main()` catches only `ValueError` from `load_manifest`** while structural manifest errors raise `TypeError` (documented ruff TRY004 deviation): a structurally malformed manifest would die with a traceback and exit 1 instead of the documented exit 2. Rejection is still loud and the path is never exercised by the shipped fixture; minor robustness/doc inconsistency.
5. **`--max-new-tokens` is a CLI override** (default 512): the 512 guardrail is default-enforced rather than hard-pinned. Both the archived and regenerated runs record `max_new_tokens: 512`, so no binding constraint is violated; noted for completeness.
6. **Cosmetic**: archived transcript's final line prints an absolute JSON path (run with `--json-out` from another cwd) while the canonical repo-root invocation prints a relative one; no cwd-dependent content otherwise (deviation #5 of the report is accurate).

### Final justification
Every acceptance criterion was re-derived independently of the implementer's claims: I re-ran the full CPU and GPU suites (229 + 34, both exit 0), re-ran the demo-parity tests, re-executed the evidence harness from the exact committed script on the real GPU (exit 0, 24/24 matrix cells + 4/4 references, `RESULT: PASS`), programmatically validated the JSON schema and its cell-by-cell agreement with the teed transcript for both the archived and regenerated artifacts, byte-checked the transcript rule against the manifest, cross-checked the language set against a fresh live `get_supported_languages()` probe (11 runtime ids incl. `auto`; the 10 official languages exactly covered), verified the official-API signatures and keyword-first `max_new_tokens=512` convention in code, audited all diff'd docs for claim discipline (only disclaimers found; README/CN tables render exactly from the evidence), confirmed the CHANGELOG entry sits under 0.2.0/Added, and hash-verified the installed qwen-tts pip RECORD (intact) with zero upstream/site-packages touches in the diff. The six problems found are cosmetic or process-level (re-stage the regenerated evidence before commit) and none violates a binding constraint or acceptance criterion. **PASS.**
