# v0.2.1 FINAL INDEPENDENT RELEASE VERIFICATION — gfx1151 production closure

- **VERDICT: PASS_WITH_CONCERNS** — every substantive release gate independently reproduces green (full CPU + GPU suites re-run live, ruff clean, all public numbers traced to artifacts, zero-upstream-patch semantics verified to the byte, upstream status re-queried live, CI unweakened); exactly one discrepancy class survives, D1 below: the automated-test counts in BOTH READMEs (and one "current claim" phrase in one evidence/README.md index row) are stale — they say 353/353 = 313 CPU + 40 GPU while the suite, the archived final regression, the CHANGELOG [Unreleased] section and the closure report all say 357/357 = 317 CPU + 40 GPU. The stale numbers UNDERSTATE coverage (no overclaim anywhere), and a one-commit docs truing in the release-notes/version-bump step clears the concern. Nothing else blocks; the user's explicit tag/publish approval remains the only remaining gate action (plus the separately user-gated unfiled upstream issue).
- Verifier: final independent release-verification subagent (assumed everything could be wrong; nothing taken on faith from the program's own reports).
- Repo: /home/amd/Desktop/Qwen3-TTS-ROCm, branch main, clean tree, HEAD `6d782a6b46c7de48c053e84c15248454ab8ea624` (branch in sync with origin/main). No tracked files modified by the verifier; writes = this report + /tmp scratch only.

## Check 1 — Test counts (all four commands re-run live) — PASS

- CPU suite, `.venv/bin/python -m pytest -m "not gpu" -q` → verbatim tail: `317 passed, 40 deselected, 2 warnings in 16.83s` — matches expectation (317/40) and the archived `evidence/final-regression-v0.2.1-2026-09-25.txt` (`317 passed, 40 deselected, 2 warnings in 16.90s`).
- GPU collection, `.venv/bin/python -m pytest -m gpu --co -q` → verbatim: `40/357 tests collected (317 deselected) in 4.17s` — matches expectation (40/357).
- FULL GPU suite (optional run, executed): `.venv/bin/python -m pytest -m gpu -q` → verbatim: `40 passed, 317 deselected, 5 warnings in 312.31s (0:05:12)`, exit 0 — independently reproduces the archived GPU half (40 passed in 318.14 s). Combined fresh result: 317 + 40 = 357/357 green on this host, today.
- Lint, `.venv/bin/python -m ruff check .` → verbatim: `All checks passed!` (exit 0).

## Check 2 — Public-claims audit (README.md + README_CN.md vs artifacts) — PASS except D1

Every spot-checked claim traces to an existing artifact whose numbers match:

- Batch inference B=8, five families — `evidence/batch-inference-gfx1151-2026-09-24.json` contains aliases `base, base-0.6b, custom-voice, custom-voice-0.6b, voice-design`; `.txt` states `per_family_max_stable: ALL FIVE = 8 (20/20 cells ok=true; every output finite, non-silent RMS>=1e-3, 24000 Hz, durations within [0.5,60] s)` ✓.
- Benchmark v2 medians 1.04–1.52, stdev ≤ 0.11 — `evidence/benchmark-v2-gfx1151-2026-09-24.txt` per-cell RTF medians sorted: min 1.04 (custom-voice-0.6b cn/short) … max 1.52; max per-cell stdev 0.11 ✓ (20 cells, n=5 each).
- Soak 815/815 — `evidence/soak-gfx1151-2026-09-24.txt`: `{"kind": "soak_summary", "stop_reason": "deadline", "total_requests": 815, "successes": 815, "failures": 0, "session_minutes": 60.04, "exit_code": 0}`, `815/815 requests OK over 60.04 minutes — ZERO correctness failures.`, RSS first-10 avg 2.77 → last-10 avg 2.79 GiB (+0.02), recycle `total_requests: 10, successes: 10` with RSS plateau 2.90→2.92 ✓.
- Ladder 8/8 tiers ≤892 chars — `evidence/long-text-gfx1151-2026-09-24.txt`: attempt-3 table `| en | very-long | 892 | 75.53 | 57.84 | 1.31 | 8192 | False |`, 8/8 green, natural EOS, `NO tier hit its token budget (hit_cap=False everywhere)`, no maximum-length claim ✓.
- Fine-tuning 1.7B+0.6B execution-only, BOTH blockers named — both README rows say "execution-only smoke … NO convergence / quality / multi-speaker claims" and name (1) PR #373 (OPEN; flash-attn hardcode, issue #372) and (2) the 0.6B text-projection omission in sft_12hz.py; evidence `finetune-smoke-2026-09-20.txt` + `finetune-06b-gfx1151-run1/run2-2026-09-24.txt` both end `RELOAD-AND-SYNTHESIS-OK`; `upstream-372-root-cause.md`, `upstream-372-e2e-run1/2.txt` exist and are linked ✓.
- Quality v2 9/10 at 0.00–0.05 + German 0.5455 — `evidence/quality-v2-gfx1151-2026-09-24.txt`: `9/10 languages score 0.0000–0.0500 error rate (zh/ko/ru CER 0; en/fr/pt/es/it WER 0; ja CER 0.05). German WER=0.5455 …` (READMEs round to 0.55); clone cosines in-JSON/verbatim: positives 0.6619/0.6296 (+cross 0.6794) > negatives 0.5776/0.6001 → README "positives 0.63–0.68 > negatives 0.58–0.60" ✓.
- vLLM serving 3 families — `evidence/vllm-online-serving-gfx1151-2026-09-24.txt`: `=== 9A CUSTOMVOICE NON-STREAMING SERVER — GREEN ===`, `=== 9B VOICEDESIGN SERVING — GREEN ===`, `=== 9C BASE VOICE-CLONE SERVING — GREEN after 3 honest client-side iterations ===` ✓.
- Streaming TTFA 0.249/0.222 — `evidence/vllm-streaming-gfx1151-2026-09-24.txt`: `TTFA 0.249s · ttfa/wall 0.115 → STREAM-VERDICT: INCREMENTAL` (22 chunks, 1 underrun) and `453-char input: TTFA 0.222s · ttfa/wall 0.005 → INCREMENTAL · 389 chunks … underruns 2` ✓.
- Concurrency c=8 seq1 + default collapse — `evidence/vllm-concurrency-gfx1151-2026-09-25.txt`: default `c=2 (24.4 req/min; c>=4 collapses into queueing: c=8 E2E p50 273 s)`; stage-1 seq1 `c=8 is stable and useful (TTFA p50 0.74 s, E2E p50 8.63 s, 34.9 req/min, 1.50 audio-s/wall-s) … NO failures / OOM / HIP errors at any level` ✓ (README "E2E 8.6 s" rounds 8.63).
- Docker 16/16 — `evidence/docker-gpu-e2e-gfx1151-2026-09-24.txt/.json`: 16 real check records green (with the disclosed 17/17→16/16 miscount erratum), 0.6B synthesis `3.84s in [0.5, 60]`, WAV archived; in-container pytest slice `4 passed in 38.33s` ✓.
- Full-weekly run 35964051504 — `evidence/gpu-ci-full-weekly-first-green-2026-09-24.txt`: `Run: 35964051504 … headSha=09ca9fd9…`, `38 passed, 313 deselected, 5 warnings in 314.08s`, benchmark table = exactly 12 rows (3 aliases × cn/en × short/medium), median RTF min 1.27 / max 1.40, artifact persisted ✓.
- v1 performance table — `evidence/benchmark.json` median_rtf per alias: custom-voice 1.3074–1.513 (README 1.31–1.51), voice-design 1.2745–1.6174 (1.27–1.62), base 1.7134–1.8778 (1.71–1.88); the 2026-09-21 base replication `benchmark-2026-09-21.json` base medians 1.2748–1.4017 (README 1.27–1.40) ✓.
- Voice-studio phases — `voice-workflow-2026-09-20.json`: design 5.469 / prompt 0.307 / reuse 5.283 & 6.417 s (README ≈5.5 / ≈0.3 / 5.3–6.4) ✓.
- **D1 (the one discrepancy class):** README.md and README_CN.md still say the suite is "353 / 353 on validation host — 313 CPU + 40 real-GPU" (Verified-results table), "The CPU-only CI matrix collects the same 313 CPU tests", "Test suite — 353/353 … (313 CPU + 40 real-GPU)", and the re-run block comments "# 313 CPU tests" / "# 38 on-GPU tests" (EN README.md lines 43, 174–181, 325–326, 532–533; CN README_CN.md lines 36, 155–160, 293, 465–466 mirror these). Root cause established precisely: Task 4 commit `64ab7f5` (2026-09-24) added `tests/test_benchmark_v2.py` (+4 CPU tests, stated in its own commit message), growing the suite 313+40=353 → 317+40=357, but no later commit trued the READMEs; the CHANGELOG knew it contemporaneously (line 447: "tests pin the stats helper (suite: 317 CPU + 40 GPU)" and line 351: "317 CPU + 40 GPU = 357/357 green") and the closure report quotes 357/357 — so the READMEs contradict both their own artifact trail and the rest of the doc set. Also same class: `evidence/README.md` row for `gpu-suite-2026-09-20.txt` says "(superseded: current claim 351/351 = 313 CPU + 38 GPU …)" — the word "current" is stale (that was the 2026-09-21 state). Note the "38 on-GPU tests" comment additionally contradicts the 40-GPU figure stated elsewhere in the very same READMEs. All stale numbers UNDERSTATE the actual verified suite (357/357 green) — an undercount, not an overclaim; no reader is misled into thinking more was verified than was.

## Check 3 — Link/path resolution across the four public docs — PASS (one expected self-reference)

Custom checker over README.md, README_CN.md, evidence/README.md, docs/gfx1151-production-closure-v0.2.1.md: `checked 201 relative markdown links across 4 docs` → single unresolved target: `docs/gfx1151-production-closure-v0.2.1.md:109: superpowers/reports/pc021-final-release-verification.md` — the closure report's forward reference to THIS report, which did not exist at gate start and exists as of this verification; not a defect. Backtick-path pass: `checked 151 distinct backtick path tokens` — all resolve to real files in the repo, evidence/, the pinned upstream clone, or retained gitignored scratch trees; the only non-existent strings are the quickstart's user-created `hello.py` example and 2026-09-21 run scratch (`train_raw.jsonl` under cleaned `.work-finetune/e2e-r*` trees, described as gitignored scratch in the very sentences that mention them). Anchor pass: `ALL ANCHORS RESOLVE` (incl. README→docs/finetuning-rocm.md#06b-base-execution-validation-v021-task-6-2026-09-24, which matches GitHub's slug of the real heading "## 0.6B Base execution validation (v0.2.1 Task 6, 2026-09-24)").

## Check 4 — Version consistency (gate state: v0.2.1 not yet cut) — PASS

- `pyproject.toml` line 8: `version = "0.2.0"` ✓.
- `CHANGELOG.md` headings: only `## [0.2.0] - 2026-09-22 — Radeon Reference Closure …` (line 8, top released section), `## [Unreleased]` (line 343), `## [0.1.0] - 2026-08-27` (line 532) — NO `[0.2.1]` heading; all ten v0.2.1 program entries live under [Unreleased] (top entry: "v0.2.1 gfx1151 production closure (2026-09-25) … READY pending the final release-verification subagent and the user's explicit approval to tag/publish") ✓ — exactly the expected pre-release state; the version bump is correctly deferred until after this PASS.
- Observation (not a defect, repo convention): this project places the dev [Unreleased] block BELOW the top released section (between [0.2.0] and [0.1.0]) — layout unchanged since the v0.2.0 release commit `4a7f1be`, i.e., not a v0.2.1-program deviation from Keep-a-Changelog ordering, just the repo's established idiosyncrasy.

## Check 5 — Upstream status, live re-query (2026-09-24) — PASS, disclosures remain accurate

- `gh api repos/QwenLM/Qwen3-TTS/issues/372` → `{"state":"open","closed_at":null,"comments":0,"title":"finetuning/sft_12hz.py hard-codes attn_implementation=\"flash_attention_2\" — official fine-tuning fails out-of-the-box on ROCm (no flash-attn build)", …}` — still OPEN, untouched.
- `gh api repos/QwenLM/Qwen3-TTS/pulls/373` → `{"number":373,"state":"open","merged":false,"merged_at":null,"title":"fix(finetuning): make attention implementation configurable"}` — still OPEN and NOT merged, so the README/CHANGELOG wording "PR #373 (OPEN)" and the "until it merges … temporary workaround" disclosure remain truthful; had it merged, the workaround-disclosure requirement would have changed — it has not.
- `gh api repos/vllm-project/vllm-omni/commits/main --jq .sha` → `7b4dc7633bde8642f0c05f77bd018751541df404` — upstream main has drifted past the evidence-pinned `7e5897b…` (expected; Task 8 evidence records 7e5897b as the exact SHA it validated, so the drift does not invalidate any pinned claim).
- Newest QwenLM/Qwen3-TTS items (state=all, sort=created desc): #373 (PR, 2026-09-21, AIwork4me), #372 (issue, 2026-09-20, AIwork4me), then #371 (2026-09-16, thewh1teagle) and older — confirms NO new upstream issue/PR was filed from this program during the v0.2.1 closure days (2026-09-24/25), matching the known-gaps statement that the text-projection issue is drafted but unfiled.

## Check 6 — Zero-upstream-patch semantics — PASS (verified to the byte)

- (a) Pristine clone: `git -C .upstream/Qwen3-TTS rev-parse HEAD` → `022e286b98fbec7e1e916cb940cdf532cd9f488e` (the pinned SHA) and `git status --porcelain | wc -l` → `0` — pristine.
- (b) Installed official package unmodified: `.venv/bin/python -m pip check` → `No broken requirements found.` (exit 0); `grep -rn "LOCAL DISCLOSED WORKAROUND" .venv/lib/python3.12/site-packages/qwen_tts/` → empty; stronger than the requested grep — I recomputed the sha256 of every file in `qwen_tts-0.1.1.dist-info/RECORD`: `checked 19 qwen_tts files against RECORD hashes` → `INSTALLED qwen_tts 0.1.1 IS BYTE-IDENTICAL TO ITS WHEEL RECORD (unmodified)`. The marker string exists in exactly two places on disk: `.upstream/Qwen3-TTS-fix/finetuning/sft_12hz.py` (the gitignored fix worktree — the disclosed workaround) and `docs/superpowers/reports/pc021-task-6-verification.md` (documentation of that workaround; expected).
- (c) Workarounds confined to gitignored worktrees: `.upstream/Qwen3-TTS-fix` sits at `512db9bf65795aa8b977f3132b3fe906c4a8b0c0` on branch `fix/finetuning-06b-text-projection` with clean porcelain; its diffs vs the pinned upstream are confined to upstream's own files — `git diff 022e286..512db9b --stat` → `finetuning/sft_12hz.py | 22 ±` + `tests/test_sft_attn_implementation.py | 105 +` (this branch carries BOTH the SDPA/attn flag from `fix/finetuning-attn-implementation` — that branch's diff is 15 ± on sft_12hz.py + the same test file — and the text-projection one-liner `input_text_embedding = model.talker.text_projection(model.talker.model.text_embedding(input_text_ids)) * text_embedding_mask`); `.gitignore` covers `.upstream/`, `.work-finetune/`, `.work-vllm/`, `.work-quality/`, `.work-upstream/`; no tracked source carries any upstream patch (src/qwen3_tts_rocm/loader.py's `resolve_attn` sdpa-on-HIP default is the loader's public-kwarg smart default, not an upstream modification; `git ls-files` has nothing under .upstream/).
- (d) Runtime separation: `ls .venv/lib/python3.12/site-packages/ | grep -iE "vllm|omni"` → no matches — the official .venv contains zero vllm/vllm-omni packages; the vLLM leg lives only in the gitignored `.work-vllm/venv30` per the evidence.

## Check 7 — Streaming-claim separation (Rule 3) — PASS

- Both READMEs' qwen-tts row today: README.md line 76 / README_CN.md line 68 — "🚫 not exposed upstream — measured 2026-09-21 on gfx1151: qwen-tts 0.1.1's official Python API delivers audio only at completion (exactly 1 chunk every run; TTFB == total wall)", linked to `evidence/streaming-2026-09-21.txt`.
- Git archaeology (`git log -G "True streaming inference" -- README.md`): the row has existed in exactly TWO states since creation — `8815238`: "🚫 not claimed — no Radeon measurements exist", and `6df5e86`: the current measured-non-incremental wording. It was NEVER rewritten to claim streaming.
- The vLLM rows (README.md line 74 / README_CN.md line 66: "vLLM-Omni true streaming (HTTP PCM) … TTFA 0.249 s / 0.222 s") are explicitly a different runtime with their own measured numbers and own evidence files; the roadmap "True streaming inference" section and the serving guide (`docs/vllm-omni-gfx1151-serving.md`) keep the same separation; grep of streaming wording across both READMEs + docs found no sentence attributing vLLM-Omni streaming to the qwen-tts Python API or vice versa. Rule 3 holds in every public surface.

## Check 8 — Closure-report accuracy (docs/gfx1151-production-closure-v0.2.1.md) — PASS

- Baseline SHA real: `git cat-file -e cd46a8152a56e0d475512ba8e86c80b46ae0dbe2` → exists (commit "docs: gpu-ci-enablement + procurement-evaluation verifier reports"); its frozen transcript `evidence/gfx1151-production-closure-baseline-2026-09-24.txt` reproduces every quoted fact (HEAD cd46a81, `313 passed, 38 deselected in 18.07s`, `38/351 tests collected`, #372 open, #373 open/merged:false, head 48b8644).
- Final SHA lineage real: `72676c39dfd9e8c81c6b64ab49451c6265274825` exists and `git merge-base --is-ancestor` confirms it is an ancestor of HEAD (it is HEAD~1; the closure-report commit 6d782a6 sits on top); `git log cd46a81..HEAD` = 44 commits, all on main.
- Task table vs the ten verifier reports' final verdict lines — all ten match exactly: Task 0 "FINAL VERDICT (after fix round 1): PASS"; Task 1 "VERDICT: PASS"; Task 2 "Fix-round verdict: PASS" (round-1 PASS_WITH_CONCERNS, 17/17→16/16 erratum); Task 3 "Final verdict: PASS" (round-1 PASS_WITH_CONCERNS, runbook-table fix); Task 4 "Verdict: PASS" (480/480 stats recomputed); Task 5 "Fix-round verdict: PASS" (round-1 PASS_WITH_CONCERNS, 8192-default + drift disclosure); Task 6 "FINAL VERDICT (after fix round 1): PASS"; Task 7 "PASS." (first round, raw-WAV re-transcription); Task 8 "## VERDICT: PASS" (rerun WAV sha-identical); Task 9 "Fix round verdict: PASS … upgraded from PASS_WITH_CONCERNS to PASS"; Task 10 "VERDICT: PASS".
- Known-gaps section matches reality: WebSocket client broken (four verbatim attempts archived in the streaming evidence); 0.6B vLLM variants untested (stated in the Task 8 evidence); PR #373 OPEN (re-verified live today); text-projection issue drafted at `.work-upstream/issue-draft-sft-06b-text-projection.md` — file exists, header reads "# DRAFT — NOT FILED (user-gated outward action) … Do not file without explicit user approval" — and the live GitHub query above confirms nothing was filed; Base concurrency matrix not run + RSS-sampling invalidity (both stated in the Task 10 evidence); soak ~15 % latency drift (soak evidence: block-mean wall +14.6 %, zero failures); German 0.5455 recorded unblamed (quality evidence).

## Check 9 — CI state (gpu-nightly unweakened since baseline cd46a81) — PASS

`git diff cd46a81..HEAD -- .github/workflows/gpu-nightly.yml`: the four pytest invocations are byte-identical at both ends — `-m gpu -q -k "official_demo or tokenizer"`, `-m gpu -q -k "custom_voice or voice_design or voice_clone"`, `-m gpu -q -k "voice_workflow"` (nightly slices), `-m gpu -q` (full-weekly) — no marker, filter, or flag weakened, no steps removed, no continue-on-error added. The complete diff is: (1) comment-block updates (38→40 nodes, 32→34 nightly slice count, and an honest supply-chain note that the workflow now has ONE first-party exception); (2) a step-name label "all 38"→"all 40"; (3) one ADDITIVE post-test step "Persist benchmark JSON as run artifact" (`actions/upload-artifact@v4`, `if: always()`, `if-no-files-found: warn`, retention 365 clamped to repo-max 90 per the evidence) — added by v0.2.1 Task 1 for durable benchmark persistence, runs after all test steps, touches no code, weakens no gate.

## Discrepancies (complete list)

- **D1 — stale suite counts in the public READMEs (blocking the CLEAN PASS, not blocking release substance).** README.md (lines 43, 174–181, 325–326, 532–533) and README_CN.md (mirror lines 36, 155–160, 293, 465–466) state 353/353 = 313 CPU + 40 GPU, "collects the same 313 CPU tests", and a re-run comment of "38 on-GPU tests". True state: 317 CPU + 40 GPU = 357/357 (live today, and archived in evidence/final-regression-v0.2.1-2026-09-25.txt; CHANGELOG lines 351/447 and the closure report already carry 357/317). Introduced by Task 4 commit 64ab7f5 (+4 CPU tests, no README truing); the "38 GPU" comment was already superseded within the same READMEs by their own "40" figures. Related same-class instance: evidence/README.md's gpu-suite-2026-09-20.txt row still says "superseded: current claim 351/351 = 313 CPU + 38 GPU" — "current" is stale (351 was the 2026-09-21 state). Direction of error: UNDERSTATEMENT of verified coverage; no overclaim. Fix: one docs commit truing 353→357, 313→317, 38→40 and the "current claim 351/351" phrase — fold it into the release-notes/version-bump commit.
- No other discrepancies found in any check. (Informational only: vllm-omni main has drifted to 7b4dc7633bde — expected, evidence correctly pins 7e5897b; CHANGELOG [Unreleased] sits below [0.2.0] per the repo's own convention since the v0.2.0 release; Task 1's verifier-recorded cosmetics — runbook "365-day" parenthetical vs the 90-day repo clamp, stale runbook header date — were disclosed in that task's report and do not gate.)

## Release-readiness statement

**v0.2.1 is substantively READY to release**: the full test matrix re-ran green live on the validation host today (317 CPU + 40 GPU = 357/357, ruff clean), every public capability number traces to an existing verbatim artifact, the zero-upstream-patch guarantee holds to the byte (pristine pinned clone; installed qwen-tts 0.1.1 RECORD-hash-identical; workarounds confined to gitignored worktrees; vLLM strictly runtime-separated), upstream disclosures (#372 OPEN, PR #373 OPEN/unmerged) remain accurate as of today, Rule-3 streaming separation is intact across all public surfaces, the closure report's task table matches all ten independent verifier verdicts, and CI has not been weakened since the program baseline. Before tagging, the release-notes commit should include the D1 one-commit docs truing (README.md + README_CN.md counts 353/313/38 → 357/317/40, and the evidence-index "current claim" phrase) so that no public surface contradicts the release-day artifacts. The two remaining actions are explicitly the user's: (1) approval to tag/publish v0.2.1 (this program does not self-approve), and (2) the still-unfiled upstream issue `.work-upstream/issue-draft-sft-06b-text-projection.md` (file or continue to hold).

## Fix round 1 (commit 530aca4, 2026-09-25) — scoped re-verification

- Commit scope verified: `git show 530aca4 --stat` touches exactly README.md, README_CN.md, evidence/README.md and adds this report (numstat 81/0 — pure addition; verdict line intact in the committed copy; committed content identical to the working-tree file). No collateral files.
- README.md (EN) — FULLY TRUED, artifact-accurate: validation-summary table now "**357 / 357 on validation host** — 317 CPU + 40 real-GPU"; CI paragraph "collects the same 317 CPU tests" and the rewritten sentence "giving 317 CPU + 40 GPU = 357 / 357 today (grew across 2026-09-24/25: +2 batch-inference GPU tests, +4 benchmark-v2 CPU tests)"; test-suite bullet 357/357 + 317; reproduce block "# 317 CPU tests" / "# 40 on-GPU tests". The new wording matches the archived `evidence/final-regression-v0.2.1-2026-09-25.txt` exactly (CPU `317 passed, 40 deselected in 16.90s` + GPU `40 passed, 317 deselected in 318.14s` = 357/357), and the growth attribution matches git history (Task 3 `15c80c7` +2 GPU tests, Task 4 `64ab7f5` +4 CPU tests). Remaining EN numbers are correct as history/present fact: "all 38 GPU test nodes passed in 314 s" for run 35964051504 (archived transcript: `Full GPU regression (all 38 gpu-marked nodes) … 38 passed, 313 deselected, 5 warnings in 314.08s`), "the 313-CPU era had been red" (era name), "one of the 40 GPU tests" (present tense, today's suite).
- evidence/README.md — TRUED: the "superseded: current claim 351/351" phrase is now the dated "superseded: the suite has since grown — 357/357 = 317 CPU + 40 GPU as of 2026-09-25; the then-latest full 38-test GPU run …" — no false "current", accurate against the transcript.
- README_CN.md — PARTIALLY trued; FOUR residual defects remain (R1–R3 same understatement class as the original D1; R4 is a NEW artifact contradiction in the overstatement direction introduced by this fix):
  - **R1** line 155: "CPU-only CI 在 Python 3.10 / 3.11 / 3.12 上收集的是同样这 313 项 CPU 测试" — stale; EN mirror (line 174) was trued to 317, this CN sentence was not.
  - **R2** line 294: "CPU-only CI 在 Python 3.10 / 3.11 / 3.12 上收集同样这 313 项 CPU 测试" — stale; EN mirror (line 326) was trued to 317.
  - **R3** line 465: reproduce-block comment "# 313 项 CPU 测试" — stale; EN mirror (line 532) was trued to 317. (The adjacent GPU line WAS trued to 40, making the same code block internally inconsistent.)
  - **R4** line 174: "提交 `09ca9fd`：全部 40 项 GPU 测试节点 314 秒内通过" — this describes run 35964051504, whose archived transcript records "all 38 gpu-marked nodes … 38 passed, 313 deselected in 314.08s" (the run predates Task 3's +2 batch tests); the EN mirror correctly says "all 38 GPU test nodes passed in 314 s" and the closure report Task 1 row says "38 nodes 314 s". The 38→40 change therefore overstates the specific archived run and contradicts both the artifact and the EN mirror. Correct fix: restore 38 (a present-tense "40" claim belongs to wording like "one of the 40 GPU tests", as at line 301, which is fine because it refers to today's suite, not to that run).
  - The fix commit's claim "grep confirms no remaining 353/313-CPU instances in either README" does not hold for README_CN.md — lines 155, 294 and 465 still contain "313 项 CPU 测试" (presumably the implementer's grep pattern did not match the CJK "313 项" form).
- All other round-1 findings stand unchanged (checks 1, 3–9 unaffected by this docs-only commit; no code, workflow, evidence or test files touched).

## FINAL VERDICT (after fix round 1): PASS_WITH_CONCERNS

Every substantive release gate remains green and independently verified (357/357 live on this host, ruff clean, zero-upstream-patch to the byte, upstream disclosures accurate, Rule 3 intact, CI unweakened, ten task verdicts reproduced); the fix commit fully cleared D1 in README.md and evidence/README.md, and the new EN wording matches the archived final-regression transcript. The remaining concerns are confined to README_CN.md and are fully enumerated, deterministic one-line fixes: true "313 项 CPU 测试" → "317 项" at lines 155, 294 and 465, and restore the run-35964051504 node count from 40 to 38 at line 174 (artifact: 38 passed in 314.08 s; EN mirror and closure report agree). Because R4 overstates an archived run, this verifier cannot issue a clean PASS until those four characters-class edits land; once they do — they require no further verification round, being mechanical and fully specified above — the release-readiness statement of round 1 applies unchanged: v0.2.1 is READY pending the user's explicit tag/publish approval and the separately user-gated unfiled upstream issue (`.work-upstream/issue-draft-sft-06b-text-projection.md`).
