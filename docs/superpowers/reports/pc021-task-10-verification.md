# PC021 Task 10 — VLLM-OMNI CONCURRENCY/THROUGHPUT CHARACTERIZATION — Independent Verification

**VERDICT: PASS** (verifier: independent subagent, 2026-09-24; repo HEAD e8b1122, branch main, tree clean during verification; no tracked files modified; official `.venv` never touched — all live work via the isolated `.work-vllm/venv30` stack)

Every quantitative claim in the Task 10 evidence reproduces exactly from the per-request JSON rows, the default-config c=8 collapse carries an authentic queueing signature in the raw rows, the variant config differs from the packaged deploy yaml by exactly one staged change, and a fresh bounded live rerun of the stage1seq1 server reproduced structural success (4/4 OK, exit 0) with teardown leaving the GPU byte-identical to baseline. One minor evidence-chain nit is recorded in Check 4 (the server logs do not echo the `--deploy-config` path); it does not undermine the config-legitimacy conclusion, which is established by three independent lines (see below).

## Check 1 — Recompute from per-request JSON (core gate): PASS

Method: for both configs and every level, recomputed from `levels[*].per_request` (ok-only rows) successes/failures, TTFA/E2E p50/p95 via `statistics.quantiles(n=100, method='inclusive')`, `requests_per_minute`, `audio_per_wall_second`, `audio_seconds_total`, and compared against the stored level aggregates, then against the summary JSON, the `.txt` tables, `docs/vllm-omni-gfx1151-serving.md`, README.md:75, README_CN.md:67, `evidence/README.md:100`, and CHANGELOG.md:347-357.

Per-config JSONs (`evidence/vllm-concurrency-{default,stage1seq1}-2026-09-25.json`): **80 aggregate fields checked, 78 exact matches, 2 discrepancies of ±0.01 in `requests_per_minute` only** (`default` c=2 stored 24.36 vs 24.3655 recomputed from the stored 2-decimal wall; `stage1seq1` c=8 stored 34.87 vs 34.8584). Both are fully explained by the probe's own code path (scripts/vllm_concurrency_probe.py:174): rpm is computed from the UNROUNDED wall (`round(len(oks)/(wall/60), 2)`) and only then is `level_wall_s` stored rounded to 2 decimals (line 175); recomputation from the rounded wall shifts rpm by ±0.01. Verified numerically: stored rpm 24.36 implies wall 19.7044 s (stored 19.70); stored rpm 34.87 implies wall 13.7654 s (stored 13.77). Not a data-integrity issue.

Recomputed values (matching all stored aggregates within the above rounding artifact): default c=1 0.207/0.991 TTFA p50/p95, E2E 3.732/9.850, audio 20.56 s, a/w 0.528, rpm 12.32, wall 38.95 s; c=2 0.296/0.352, 4.881/5.600, 22.64, 1.149, 24.36, 19.70 s; c=4 0.435/6.148, 35.826/116.410, 22.48, 0.180, 3.83, 125.23 s; c=8 15.098/15.101, 273.332/273.333, 21.52, 0.079, 1.76, 273.34 s. stage1seq1 c=1 0.212/1.420, 3.705/9.790, 20.56, 0.530, 12.37, 38.81 s; c=2 0.283/0.337, 4.311/6.320, 22.32, 1.083, 23.29, 20.61 s; c=4 0.466/0.510, 6.413/8.539, 22.96, 1.363, 28.49, 16.85 s; c=8 0.739/0.959, 8.628/13.218, 20.64, 1.499, 34.87, 13.77 s. Summary JSON (`...gfx1151-2026-09-25.json`, `rows` schema with `ok` as "8/8" strings): **56/58 fields match** with the same two explained rpm deltas. Every derived number quoted in the `.txt` transcript, docs table, both README capability rows, the evidence index row, and the CHANGELOG entry matches these recomputed values at the precision printed (docs round to 2 significant figures: 15.1 s, 273.3 s, 34.9 req/min, 1.50 audio-s/wall-s — all consistent).

## Check 2 — c=8 default E2E authenticity: PASS

Raw `per_request` e2e_s at default c=8: [168.237, 273.327, 273.327, 273.332, 273.332, 273.333, 273.333, 273.333] with level wall 273.34 s — seven of eight requests complete in a single near-simultaneous burst at the very end of a ~4.6-minute level, exactly the interleaving/queueing-collapse signature (all requests admitted at once under `max_num_seqs:64`, decode thrashes, audio is delivered only when the batch finally drains). TTFA rows show 13.055-15.102 s for seven requests (queued first-chunk wait) with one lucky 0.287 s. c=4 shows the same pattern in miniature: completion bursts at ~8.8 s (4 requests), ~62.8 s (2), ~116.4 s (2). Internal consistency also holds everywhere: bytes/audio_s = 48000 B/s exactly (24 kHz × 2-byte PCM; e.g. 92160 B → 1.92 s), and no per-request e2e exceeds the level wall. These are real measurements, not fabricated aggregates.

## Check 3 — Prompt determinism and ladder: PASS

Both JSONs, all 8 levels (4 per config): exactly 8 requests per level; the same fixed 8-prompt set as `PROMPTS` in scripts/vllm_concurrency_probe.py:44-53, cycling correctly (`prompts == prompts[i % 8]`); `ladder_stopped_at: null` in both files and in both summary-JSON legs; `failures: 0` and `failures_verbatim: []` at every level — consistent with the "zero failures/OOM/HIP errors" claim and the never-triggered stop-on-failure policy.

## Check 4 — Variant-config legitimacy: PASS with one evidence-chain nit

`diff .work-vllm/venv30/lib/python3.12/site-packages/vllm_omni/deploy/qwen3_tts.yaml .work-vllm/qwen3_tts_stage1seq1.yaml` → exactly one hunk: line 111 `max_num_seqs: 64` → one comment line + `max_num_seqs: 1`. The changed line sits inside the `stage_id: 1` block (stage-1 starts at line 107); stage-0 `max_num_seqs: 64` (line 70) is untouched, and no other field differs. The upstream note the variant relies on is verbatim in the PACKAGED yaml at line 110: "# Tuned for Base voice clone; CustomVoice / VoiceDesign are TTFA-optimal at 1." — the implementer quoted it accurately. I additionally confirmed upstream ships `qwen3_tts_high_concurrency.yaml` with stage-1 `max_num_seqs: 10` (Base-oriented larger stage-1), corroborating the task's upstream-stage-tuning framing.

NIT: neither `.work-vllm/server-t10-default.log` nor `server-t10-seq1.log` echoes the `--deploy-config` path (the "non-default args" line is identical in both — model_tag/port/model only — and a pid/timestamp-normalized diff of the two logs shows only stats-table formatting differences; vllm-omni does not log the deploy-config or the effective max_num_seqs). So the server logs alone cannot prove which yaml each leg loaded. Compensating evidence that does establish it: (a) `.work-vllm/task10-conc.log:2` records the default leg's launch with the packaged path ("CONFIG default (deploy-config venv30/lib/python3.12/site-packages/vllm_omni/deploy/qwen3_tts.yaml)") and a separately launched stage1seq1 leg; (b) the two legs' raw per-request profiles are qualitatively different in exactly the way a stage-1 64→1 change predicts (default: c≥4 burst-collapse; seq1: monotone, E2E p50 scaling ~linearly 3.7→4.3→6.4→8.6 s — i.e., stage-1 serializes); (c) my Check-5 rerun captured the live server cmdline from /proc demonstrating the `--deploy-config <variant.yaml>` mechanism is how the variant is selected, and reproduced the seq1 behavioral profile. Conclusion: variant legitimacy established; recommendation (non-blocking): future server launches should `echo` the full invocation into the log header.

## Check 5 — Live bounded rerun: PASS

Started one fresh stage1seq1 server exactly per the Task 9/10 recipe: `.work-vllm/venv30/bin/vllm serve Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice --deploy-config /home/amd/Desktop/Qwen3-TTS-ROCm/.work-vllm/qwen3_tts_stage1seq1.yaml --omni --port 8093` with `HF_HOME=.work-vllm/hf-home HF_HUB_OFFLINE=1`; server pid 204125 cmdline captured from `/proc/204125/cmdline` proving the variant path was loaded. Ready (`/v1/audio/voices` → 200, "Application startup complete") in ~48 s (warm caches; within the 60-135 s envelope). Bounded probe: `--levels 2 --requests-per-level 4 --tag verify --json-out /tmp/task10-verify.json` → **4/4 OK, exit 0, ladder_stopped_at null**. Per-request rows: cold first pair (server's first traffic ever) TTFA 2.225 s both; warm second pair TTFA 0.246/0.338 s, E2E 4.567/5.460 s — the warm-pair TTFA reproduces both the implementer's c=2 regime (p50 0.283 s after their c=1 warmup leg) and Task 9's incremental 0.22-0.25 s. Level aggregates: TTFA p50 1.282 / p95 2.225, E2E p50 6.848 / p95 15.69, audio 10.4 s, a/w 0.463, rpm 10.68, wall 22.47 s, peak_vram 0.72 — numbers differ from the implementer's as expected (4 vs 8 requests, zero warmup vs a full c=1 warmup leg); the structural claim (all-OK, sub-second warm TTFA, seq1 monotone behavior, exit 0) reproduces. The rerun also REPRODUCED the implementer's `server_rss_gb_last: 0.0` and exposed its mechanism: the probe's `pgrep -f "vllm serve"` min-pid heuristic selected the bash wrapper (pid 204123) rather than the python API server (204125), whose tiny VmRSS rounds to 0.0 — confirming the implementer's decision to flag RSS sampling invalid and claim nothing from it. Teardown: `kill 204125`, then `pkill -9 -f` on the concatenated "Stage""EngineCore" pattern; afterwards no vllm/StageEngineCore processes remain, port 8093 connection-refused, `rocm-smi --showpids` reports "No KFD PIDs currently running", dedicated VRAM returned to exactly the pre-start baseline (767336448 B) and GTT fell to 108 MB — GPU fully freed.

## Check 6 — Env-claims sanity: PASS

My own dedicated-VRAM readings via `rocm-smi --showmeminfo vram`: idle before server start 767336448 B (0.7147 GiB), mid-serving under concurrent load 769572864 B (0.7165 GiB), idle after teardown 767336448 B — total movement ~2 MB under full serving load, and the idle baseline alone already sits at ~0.71 GiB. This independently confirms the docs' caveat that dedicated VRAM on this APU is not a utilization signal (engine memory lives in the unified GTT pool) and that the implementer's flat ~0.72 GiB readings are essentially the idle desktop baseline. Language audit: docs/vllm-omni-gfx1151-serving.md, both README rows, the evidence index row, the summary JSON `env_notes`, and the `.txt` HONEST LIMITATIONS all explicitly state dedicated VRAM is NOT a utilization signal, that no memory-pressure numbers are claimed, and that RSS sampling failed (0.0) and is not claimed — no VRAM-utilization or RSS claim exists anywhere.

## Check 7 — Docs audit at e8b1122: PASS

Commit e8b1122 contains exactly the claimed set (CHANGELOG.md +12, README.md +1, README_CN.md +1, docs/vllm-omni-gfx1151-serving.md +55, evidence/README.md +1, the three concurrency evidence files; probe script committed earlier in 4b0f736, 224 lines). All concurrency tables/rows match the recomputed numbers (Check 1). The answer is correctly host-scoped everywhere it appears: docs ("THIS host ... THIS stack, short-prompt workloads. Nothing here is a universal-best-configuration claim", "Both answers are this-host claims"), `.txt` ("NOT a universal-best-configuration claim: this host, this stack, short-prompt workload only"), summary JSON answer ("NOT a universal-best claim; this host/config only"), README ("this host only"), README_CN ("仅限本机"), CHANGELOG ("Scope: this host/stack/workload only") — and it answers the actual task question (highest concurrency stable and useful on this stack: c=8 with stage-1 seq1, or c=2 on the packaged default) rather than naming a universal best config. The docs caveat section is intact (three items: GTT/VRAM, RSS, short-prompt + non-benchmark-v2 treatment, plus the Base-matrix-not-run limitation recorded in `.txt`/index). CHANGELOG wording is accurate against the recomputed numbers. Official `.venv` contains no vllm package (`pip list | grep -i vllm` empty); the working tree stayed clean at e8b1122 through the whole verification (only the pre-existing untracked `.work-docker/` directory, present before I started).

## Discrepancies and observations

1. (Cosmetic, explained — Check 1) `requests_per_minute` stored vs recomputed-from-stored-wall differs by ±0.01 at default c=2 (24.36 vs 24.3655) and stage1seq1 c=8 (34.87 vs 34.8584): the probe rounds rpm from the unrounded wall before storing the rounded `level_wall_s`. No action needed; 78/80 + 56/58 fields match exactly otherwise.
2. (Evidence-chain nit — Check 4) The Task 10 server logs do not record which deploy yaml was loaded (vllm/vllm-omni log the omni config nowhere in these runs), so the specific sub-expectation "grep the non-default deploy-config arg from server-t10-seq1.log" cannot be met from the logs alone; legitimacy is instead established by the orchestrator log (default path named), the exact one-line diff, the divergent behavioral signatures, and my live cmdline-level rerun. Non-blocking; suggest echoing the full server invocation into future run logs.
3. (Observation) The probe's RSS sampling is structurally broken (pgrep min-pid grabs the bash wrapper, not the API server) — the implementer already flags RSS as invalid and claims nothing from it; the limitation is honestly documented in all artifacts.
4. (Observation) "Chunk cadence where streaming" is captured as TTFA p50/p95 + byte totals per request, not per-chunk timestamps; single-stream cadence is deferred to the Task 9 evidence, which the docs caveat states explicitly. Acceptable under the program's "small defensible matrix" framing, noted for completeness.

Verifier artifacts (scratch): /tmp/task10_recompute.py, /tmp/task10_summary_check.py, /tmp/task10-verify.json, /tmp/task10-verify-probe.log, /tmp/task10-verify-server.log.
