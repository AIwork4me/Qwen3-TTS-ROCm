# PC021 v0.2.1 — TASK 1 INDEPENDENT VERIFICATION: FIRST FULL-WEEKLY GPU CI GREEN

**VERDICT: PASS** (verified 2026-09-24, independent verifier; every check re-executed live against GitHub Actions API, `gh run view --log`, the downloaded artifact, and local git — no implementer output taken on faith)

Scope: Task 1 of `docs/superpowers/plans/2026-09-24-gfx1151-production-closure-v0.2.1.md` — obtain a real green `full-weekly` run on the gfx1151 self-hosted runner without weakening any test; persist the benchmark JSON as a durable CI artifact without committing it; update runbook / READMEs / evidence index / CHANGELOG. Assumed the implementer was wrong and reproduced every claim from live GitHub truth.

## Check 1 — Run metadata (`gh api .../actions/runs/35964051504`): PASS

Live API response fields: `"id":35964051504`, `"name":"gpu-nightly"`, `"run_number":5`, `"event":"workflow_dispatch"`, `"status":"completed"`, `"conclusion":"success"`, `"head_sha":"09ca9fd9bd930873d6bd4687efdc2dd9d2dc79d0"`, `"head_branch":"main"`, `"path":".github/workflows/gpu-nightly.yml"`, `"run_attempt":1`, `"created_at":"2026-09-24T06:20:55Z"`, `"updated_at":"2026-09-24T06:33:25Z"`. head_commit message matches 09ca9fd's commit message ("ci: persist full-weekly benchmark JSON as run artifact …"). Every implementer claim about the run object matches verbatim. The dispatch input `suite=full-weekly` is not exposed by the runs API endpoint, but it is proven behaviorally (see Check 2): the workflow default is `gpu-short` and the guards are `gpu-short: if github.event.inputs.suite != 'full-weekly'` / `full-weekly: if github.event.inputs.suite == 'full-weekly'` (lines 85/150 of the workflow at 09ca9fd) — full-weekly ran and gpu-short was skipped, which is only possible if the input was `full-weekly`.

## Check 2 — Jobs, runner, steps (`gh api .../runs/35964051504/jobs`): PASS

Exactly two jobs returned; nothing hidden. (a) `full-weekly`: `"id":107518621657`, `"conclusion":"success"`, `"runner_name":"amd-HP-ZBook-Ultra"`, `"runner_id":21`, `"labels":["self-hosted","radeon-gfx1151"]`, `"started_at":"2026-09-24T06:21:01Z"`, `"completed_at":"2026-09-24T06:33:24Z"` (≈12m23s wall as claimed), and all 8 steps `success`: Set up job; Sync repository (retrying fetch — host github.com is TLS-flaky); Environment diagnostics (fail fast if host prerequisites drifted); Full GPU regression (all 38 gpu-marked nodes); GPU stack sanity (fine-tune prerequisite); Benchmark replication (RTF across registered aliases); Persist benchmark JSON as run artifact; Complete job. (b) `gpu-short`: `"conclusion":"skipped"`, empty steps, null runner — visible in the API response, skipped by the documented suite guard, not a hidden failure. Self-hosted gfx1151 runner requirement: confirmed via labels.

## Check 3 — Key log lines (`gh run view 35964051504 --log`): PASS

All claimed lines exist verbatim in the live log (job/step/timestamps quoted from the live grep): `SYNCED_TO 09ca9fd9bd930873d6bd4687efdc2dd9d2dc79d0` (Sync repository, 06:21:38Z); provenance assert resolving `qwen3_tts_rocm` at `/home/amd/actions-runner/_work/Qwen3-TTS-ROCm/Qwen3-TTS-ROCm/src/qwen3_tts_rocm/__init__.py` (Environment diagnostics, 06:21:40Z) — i.e. code-under-test came from the fetched SHA's workspace, PYTHONPATH won over the host editable install (the assert step text "Code-under-test provenance: PYTHONPATH must win over the host" is in the log); `38 passed, 313 deselected, 5 warnings in 314.08s (0:05:14)` (Full GPU regression, 06:26:54Z) — all 38 GPU nodes ran; `SPIKE-GPU-OK` (GPU stack sanity, 06:27:24Z), preceded by `Run bash "$HOST_REPO/scripts/verify_gpu.sh"` so verify_gpu.sh is confirmed as the executed script; `[bench] wrote evidence/benchmark-nightly.json` (Benchmark replication, 06:33:12Z). Environment also confirmed gfx1151: `# gpu=AMD Radeon 8060S Graphics (gfx1151, multi_processor_count=20, torch-visible memory 80.0 GiB)` and `# torch=2.12.0+rocm7.14.0 (HIP 7.14.60850)`. I diffed these lines against the committed transcript `evidence/gpu-ci-full-weekly-first-green-2026-09-24.txt` (read in full, 55 lines): timestamps and content of lines 12–43 match the live log exactly, including the two disclosed warnings (retention clamp; Node.js 20 deprecation) and the artifact block.

## Check 4 — Head SHA relationship and no-test-weakening in 09ca9fd: PASS

09ca9fd is a real commit on main: `git branch -r --contains` → `origin/main`; `gh api .../commits/09ca9fd...` → sha 09ca9fd…, parent f6ec08c, message matches the run's head_commit. `git show 09ca9fd --stat` touches exactly two files: `.github/workflows/gpu-nightly.yml` (+24/−3) and `docs/development/gpu-ci-runbook.md` (+19/−8) — as claimed. The workflow diff contains only (a) a reworded supply-chain comment (disclosing the new first-party upload-artifact exception) and (b) the new `Persist benchmark JSON as run artifact` step (`actions/upload-artifact@v4`, `if: always()`, name `benchmark-nightly-json`, path `evidence/benchmark-nightly.json`, `if-no-files-found: warn`, `retention-days: 365`) appended after the benchmark step in the full-weekly job. I extracted the entire `Full GPU regression` step from both `09ca9fd~1:` and `09ca9fd:` versions of the workflow and diffed them: identical (`PYTEST_STEP_IDENTICAL`); the invocation remains `"$HOST_REPO/.venv/bin/python" -m pytest -m gpu -q` with the same env/filters — no weakening, no changed test selection. Run at head SHA 09ca9fd therefore tested exactly the code that contains this upload step (SYNCED_TO equals head_sha).

## Check 5 — Artifact metadata (`gh api .../runs/35964051504/artifacts`): PASS

Exactly one artifact: `"id":10793473404`, `"name":"benchmark-nightly-json"`, `"size_in_bytes":2497`, `"expired":false`, created 2026-09-24T06:33:18Z. Log corroborates: `Uploaded bytes 2497`, `SHA256 digest of uploaded artifact zip is 24df96d197d3250bb465ed5bd617e7e64065e942641d1898079f7c75699447d3`, `Artifact benchmark-nightly-json.zip successfully finalized. Artifact ID 10793473404`, plus the disclosed `##[warning]Retention days cannot be greater than the maximum allowed retention set within the repository. Using 90 instead.` — all matching the implementer's claims including the 365→90 clamp.

## Check 6 — Artifact download, JSON validation, not-committed: PASS

`gh run download 35964051504 -n benchmark-nightly-json -D /tmp/task1-verify-artifact -R AIwork4me/Qwen3-TTS-ROCm` succeeded (benchmark-nightly.json, 12455 bytes uncompressed vs 2497 zipped — consistent). JSON parses; `.results` has exactly 12 rows; `.meta.git_head` == `09ca9fd9bd930873d6bd4687efdc2dd9d2dc79d0` (matches run head_sha); `.meta.upstream_qwen3_tts_sha` == `022e286b98fbec7e1e916cb940cdf532cd9f488e` and `.meta.host`/`.meta.gpu` == `amd-HP-ZBook-Ultra (Linux-6.17.0-1032-oem-…)` / `AMD Radeon 8060S Graphics (gfx1151, …)` — all matching the transcript's post-run validation section. Committed-into-repo check: `git ls-files | grep -i benchmark-nightly` returns nothing (exit 1) — the JSON is not tracked, as required.

## Check 7 — Documentation accuracy (numbers cross-checked against live run data): PASS

README.md GPU CI paragraph (lines 182–198): "first green `full-weekly` run on 2026-09-24 (run 35964051504, commit `09ca9fd`: all 38 GPU test nodes passed in 314 s, `verify_gpu.sh` stack sanity green, RTF benchmark replication across the three 1.7B aliases — 12 cells, median RTF 1.27–1.40 — with the benchmark JSON [persisted as a downloadable run artifact])" plus transcript pointer — run id, SHA, 38 nodes, 314 s, 12 cells, RTF range all match live data. README_CN.md (lines 167–177) carries the same numbers in Chinese, matching. evidence/README.md line 84 adds the full-weekly row: run_number 5, event/suite, runner amd-HP-ZBook-Ultra with labels, commit 09ca9fd, `38 passed, 313 deselected in 314.08 s`, SPIKE-GPU-OK, 12 cells RTF 1.27–1.40, Artifact ID 10793473404, sha256 `24df96d1…`, retention clamp disclosed, skipped gpu-short disclosed as suite guard, JSON-not-committed policy — every number matches what I independently fetched. CHANGELOG.md `[Unreleased]` (line 343, added in 5c01fd6): new Added entry (first green full-weekly run with all the same verified numbers) and Changed entry (benchmark artifact persistence, explicitly disclosing "retention request 365 d is clamped by the repository's 90-day maximum"). Runbook go-live checklist step 5 is ✅ with run 35964051504, commit 09ca9fd, ~12½ min wall, 38 nodes in 314.08 s, SPIKE-GPU-OK, 12 cells RTF 1.27–1.40, Artifact ID 10793473404, transcript pointer — all matching; the runbook ephemerality section was updated in 09ca9fd to describe the new upload step and its `git clean` rationale. No overclaiming found: the docs say "self-hosted" + gfx1151 (true), and disclose rather than hide the skipped gpu-short job and both warnings.

## Check 8 — Nothing else weakened in 09ca9fd..5c01fd6: PASS

`git diff --stat 09ca9fd~1..5c01fd6` shows exactly 7 files, all documentation/CI: `.github/workflows/gpu-nightly.yml` (+24/−3), `CHANGELOG.md` (+22), `README.md` (+10/−1), `README_CN.md` (+10/−1), `docs/development/gpu-ci-runbook.md` (+27/−7), `evidence/README.md` (+1), `evidence/gpu-ci-full-weekly-first-green-2026-09-24.txt` (+55). No test files, no conftest, no pytest config, no markers — nothing that could weaken a test. Remote main head is 5c01fd6 (`gh api .../branches/main`), so the verified docs state is what is published.

## Check 9 — Benchmark replication really ran 12 measured cells: PASS

Live log summary table has exactly 12 data rows = 3 aliases (custom-voice, voice-design, base) × 2 langs (cn, en) × 2 lengths (short, medium), each `runs=2`: custom-voice cn/short 1.28, cn/medium 1.34, en/short 1.35, en/medium 1.29; voice-design cn/short 1.28, cn/medium 1.38, en/short 1.35, en/medium 1.27; base cn/short 1.30, cn/medium 1.36, en/short 1.31, en/medium 1.40 (median RTF column). Min median 1.27, max median 1.40 — the claimed "1.27–1.40" range is exact. The artifact JSON's unrounded values agree: min 1.2749 (voice-design en medium), max 1.4024 (base en medium). The runner also logged the per-run rows (e.g. `alias=custom-voice lang=cn len=short run=1/2 wall=4.45s audio=3.52s rtf=1.26`) so the medians are computed from real measurements, not asserted.

## Discrepancies

None material. Two cosmetic observations, neither affecting the verdict: (1) the runbook ephemerality bullet says "(365-day retention)" — that is the workflow's requested value; the effective retention is 90 days after the repository clamp, which IS disclosed verbatim in the CHANGELOG entry, the evidence index row, and the transcript's warnings section, so no reader of the doc set is misled, but the runbook parenthetical alone would suggest 365 d actual. (2) The runbook checklist header still reads "(state as of 2026-09-23)" while steps 5–6 now record 2026-09-24 events — stale header date only.

## Conclusion

Run 35964051504 is a genuine, unweakened first green `full-weekly` execution: correct self-hosted gfx1151 runner, code-under-test identical to head SHA 09ca9fd, all 38 GPU nodes passed (313 deselected CPU-side), verify_gpu.sh green, 12-cell benchmark replicated with median RTF 1.27–1.40, benchmark JSON persisted as durable artifact 10793473404 (sha256 24df96d1…) and demonstrably not committed, and every documentation claim cross-checks against live GitHub data. Task 1 requirements are fully met.
