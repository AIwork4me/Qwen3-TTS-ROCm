# pc021 Task 8 verification — VLLM-OMNI CURRENT TRUTH RUN (verifier: independent, 2026-09-24 late evening)

## VERDICT: PASS

All eight assigned checks were reproduced independently (upstream API queries, sha256 provenance, venv forensics, a full GPU rerun, soundfile measurements, log/JSON cross-reading, docs audit at ae94741). The core gate — an independent rerun of one representative request — produced a WAV that is sha256-BIT-IDENTICAL to the archived evidence (54e8a1de9fd4d7cfaf763a4f433d89a73753500460251110cbc02850c86ae65b). Three minor non-blocking observations are listed at the end (one stale roadmap parenthetical in both READMEs, one unpreserved raw log context, load time only implicitly recorded); none affects the validity of the Task 8 results or any claim made in the evidence.

Environment of this verification: repo /home/amd/Desktop/Qwen3-TTS-ROCm at HEAD ae94741 (branch main, working tree clean apart from pre-existing untracked .work-docker/), GPU idle before rerun (rocm-smi: GPU use 5%, VRAM 2%). No tracked files were modified by the verifier; all rerun outputs went to /tmp/task8-verify-vd/.

## Check 1 — Upstream SHA: PASS (drift noted, not a failure)

`gh api repos/vllm-project/vllm-omni/commits/main --jq .sha` returned `0295eeb6f3d62c7302f63e0b67c21649adb9c2c5` — main has moved past the run's pinned SHA tonight, as anticipated by the brief. The pinned SHA itself is real and queryable: `gh api repos/vllm-project/vllm-omni/commits/7e5897b8482d67800b561d6a7a5736a67c6485a1` returned `{"sha": "7e5897b8482d67800b561d6a7a5736a67c6485a1", "date": "2026-09-24T13:45:00Z", "message": "[CI/Build][Bugfix] Cover corrupt chat audio data URLs on Qwen3-Omni (#8108)"}`. Timeline consistency: new main 0295eeb6 is dated 2026-09-24T15:32:07Z ("[Orchestrator] Implement cache reset RPC forwarding to AR stage engine cores (#6766)") — i.e. the run's evidence timestamp 2026-09-24T15:30:00Z sits 1 h 45 min after the pinned commit and 2 min before the successor commit, exactly consistent with "re-queried live 2026-09-24 evening". The "was 3d43571 that morning" claim also checks out: 3d43571b is real, dated 2026-09-24T05:06:58Z. The implementer's first SHA string in the handoff (with a duplicated `d67800b561d…` segment, 59 chars) was malformed, but the corrected full string `7e5897b8482d67800b561d6a7a5736a67c6485a1` (exactly 40 hex chars) is what is recorded in every evidence artifact (log header, txt, json, CHANGELOG) and is verified real.

## Check 2 — end2end.py / recipe provenance: PASS (byte-verbatim confirmed independently)

Fetching upstream myself at the pinned ref via the contents API: `gh api "repos/vllm-project/vllm-omni/contents/examples/offline_inference/text_to_speech/qwen3_tts/end2end.py?ref=7e5897b8482d67800b561d6a7a5736a67c6485a1" --jq .content | base64 -d | sha256sum` → `58ff5abcce60499e993cd675469fe3e9036e753f5ee028946414e4164c76ea76  -`; local `sha256sum .work-vllm/omni-current-7e5897b/end2end.py` → `58ff5abcce60499e993cd675469fe3e9036e753f5ee028946414e4164c76ea76` — identical (21378 bytes). The archived recipe matches upstream byte-for-byte too: `gh api "repos/vllm-project/vllm-omni/contents/recipes/Qwen/Qwen3-TTS.md?ref=7e5897b…" --jq .content | base64 -d | sha256sum` → `b1720edc2ea57e73a220db6e846f566c4b9bacb629e686f5b4d7a0d8f7601cf5`, equal to the sha256 of `.work-vllm/omni-current-7e5897b/recipe-Qwen3-TTS.md`. The example and recipe are verbatim upstream at the pinned SHA, unmodified. The recipe also independently confirms the supported-model table claim: rows `CustomVoice | Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice`, `VoiceDesign | Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign`, `Base | Qwen/Qwen3-TTS-12Hz-1.7B-Base`, plus the line "Smaller 0.6B variants are also available for `CustomVoice` and `Base`" — exactly the "CustomVoice/VoiceDesign/Base 1.7B (+0.6B CV/Base)" recorded in the evidence.

## Check 3 — Stack versions and official .venv integrity: PASS

venv30 has no pip binary (uv-style venv), so I read importlib.metadata directly: `vllm = 0.30.0+rocm723`, `vllm-omni = 0.30.0rc1`, `onnxruntime-rocm = 1.22.2.post3` (a plain CPU `onnxruntime = 1.30.0` is also present — see observations). Note `python -c "import vllm; print(vllm.__version__)"` prints just `0.30.0` because the `__version__` attribute strips the local version segment; the distribution metadata carries the full `0.30.0+rocm723`, which is what the evidence records. No direct_url.json survives in the venv, so the wheels.vllm.ai origin rests on the dist name itself (`vllm-0.30.0+rocm723.dist-info` matches the official ROCm index naming); this is consistent and not contradicted anywhere. PyPI cross-check: release `vllm-omni 0.30.0rc1` upload_time `2026-09-23T04:48:22` — matches the claimed "PyPI 2026-09-23". Official .venv untouched: `.venv/bin/pip list | grep -i vllm` → empty (grep exit 1, no output); `.venv/bin/python -c "import qwen_tts; print('official venv intact')"` → `official venv intact` (with the expected sox noise). PASS.

## Check 4 — Independent GPU rerun (core gate): PASS, bit-identical reproduction

Command, from repo root, GPU verified idle first: `rm -rf /tmp/task8-verify-vd && mkdir -p /tmp/task8-verify-vd && HF_HOME=$PWD/.work-vllm/hf-home HF_HUB_OFFLINE=1 .work-vllm/venv30/bin/python .work-vllm/omni-current-7e5897b/end2end.py --query-type VoiceDesign --output-dir /tmp/task8-verify-vd`. Result: `RERUN_EXIT=0`; output dir contains exactly one wav `output_0_74cc0e7e-c579-45a6-83de-5f7382948142.wav` (238124 bytes); soundfile (via the repo .venv): `sr=24000 dur=4.960s finite=True rms=0.0975 peak=0.6367`. Clean staged shutdown verbatim in the rerun log: `INFO 09-24 23:59:30 [stage_pool.py:1362] [StagePool] Stage 0 replica 0 shut down` and `INFO 09-24 23:59:35 [stage_pool.py:1362] [StagePool] Stage 1 replica 0 shut down`. Decisive point: `sha256sum` of my rerun wav = `54e8a1de9fd4d7cfaf763a4f433d89a73753500460251110cbc02850c86ae65b`, IDENTICAL to `evidence/vllm-current-wavs/voicedesign-1p7b.wav` (`54e8a1de9fd4d7cfaf763a4f433d89a73753500460251110cbc02850c86ae65b`) — the archived evidence is a deterministic reproduction target, not a one-off. Wall time of my rerun: process start ~23:58:00, final StagePool shutdown 23:59:35 — roughly 95 s end-to-end including engine init, generation, and staged shutdown.

## Check 5 — Archived WAVs: PASS (all three match the JSON to 4 decimals)

soundfile via the repo .venv, independently re-measured: `customvoice-1p7b.wav: sr=24000 frames=111360 dur=4.640s finite=True rms=0.0915 peak=0.9336`; `voicedesign-1p7b.wav: sr=24000 frames=119040 dur=4.960s finite=True rms=0.0975 peak=0.6367`; `base-clone-1p7b.wav: sr=24000 frames=113280 dur=4.720s finite=True rms=0.0991 peak=0.7539`. All three finite, non-silent (peaks 0.93/0.64/0.75), 24 kHz, and durations/rms exactly equal to the JSON values (4.64/0.0915, 4.96/0.0975, 4.72/0.0991).

## Check 6 — Log consistency: PASS

`.work-vllm/task8-offline.log` (71 lines) reads exactly as claimed. Header line 1: `=== TASK 8 CURRENT-UPSTREAM OFFLINE LADDER — 2026-09-24T23:30:16+08:00 ===`; header line 2 records the stack, the pinned SHA, verbatim-fetch method, and `host gfx1151`. First ladder: line 16 `EXIT_CustomVoice=0`; line 30 `EXIT_VoiceDesign=1`; line 44 `EXIT_Base=1`; both failures carry the verbatim `huggingface_hub.errors.LocalEntryNotFoundError: Cannot find an appropriate cached snapshot folder for the specified revision on the local disk and outgoing traffic has been disabled. To enable repo look-ups and downloads online, pass 'local_files_only=False' as input.` through the `snapshot_download` traceback. Rerun marker line 47: `=== RERUN after refs fix (2026-09-24T23:40:12+08:00) ===`; line 59 `EXIT_VoiceDesign=0`; line 71 `EXIT_Base=0`. Every leg (three original + two rerun) ends with `[StagePool] Stage 0 replica 0 shut down` and `[StagePool] Stage 1 replica 0 shut down` (stage0 shutdowns show the upstream force-kill-then-complete pattern, present identically in my own verifier rerun — normal engine behavior, not an implementer anomaly). The refs-newline story is confirmed on disk: `xxd` of refs/main for CustomVoice, VoiceDesign, AND Base (dir `models--Qwen--Qwen3-TTS-12Hz-1.7B-Base` — my first probe used the wrong dir name `…-1.7B` and initially reported MISSING; corrected) all show exactly `00000000: 6d61 696e  main` — the 4 bytes `main`, no trailing newline, hex `6d61696e`. The internal logic of the story also holds: CustomVoice was green in the FIRST ladder because its refs entry was authored 2026-09-21 without a newline, while the freshly created VD/Base entries (with newline) failed — consistent with the log ordering.

## Check 7 — Evidence JSON accuracy: PASS

`evidence/vllm-omni-current-gfx1151-2026-09-24.json`: `upstream_main_sha` = the verified real 40-char SHA; `installed_stack` = vllm 0.30.0+rocm723 / vllm-omni 0.30.0rc1 (PyPI 2026-09-23) / onnxruntime 1.22.2.post3 (rocm) — all match check 3; all five `recent_qwen3_tts_commits` verified real upstream via the API with matching short-SHA, date, and PR number: `a9e2117f 2026-09-21T06:03:08Z [Core][Model] Enable Qwen3-TTS MRV2 and optimize the TTS pipeline (#7781)`, `a2138b83 2026-09-16T04:50:00Z [Feature][Qwen3-TTS] Make Code2Wav honor the stage dtype (#6059)`, `3f473a35 2026-09-23T00:10:15Z [Model] Accelerate Breeze-TTS-2 non-streaming inference (#7752)`, `5dbae2b1 2026-09-16T03:08:49Z [Bugfix] Per-key accumulation strategy for Qwen3-TTS codec frame outputs (#7608)`, `aff7d649 2026-09-10T04:08:28Z [Rebase] Rebase to vLLM 0.29.0 (#7230)`. Per-leg numbers (exit codes, wav paths, audio_seconds, sr, rms) match my independent measurements exactly; the VD/Base notes truthfully disclose the refs-newline first-attempt failures; the notes array carries the honest gaps verbatim: `no rocm-smi bookends were captured in this run — memory observations NOT recorded (honest gap; not claimed)` and `0.6B variants not tested (program says only after 1.7B families understood)`. The referenced `base_ref_audio` `src/qwen3_tts_rocm/demo/assets/ref_en.wav` exists (134444 bytes). The companion txt (`evidence/vllm-omni-current-gfx1151-2026-09-24.txt`) is consistent with the JSON and adds the scope statements (offline only; serving/streaming/perf = Task 9/10).

## Check 8 — Docs audit at ae94741: PASS on accuracy, one stale sentence found and reported (not fixed)

`git show --stat ae94741` touches exactly: CHANGELOG.md, README.md, README_CN.md, evidence/README.md, 3 WAVs, and the txt/json evidence. Capability rows: README.md line 73 and README_CN.md line 65 both now read "offline proven on the CURRENT stack (2026-09-24: vllm 0.30.0+rocm723 + vllm-omni 0.30.0rc1, upstream end2end.py verbatim): all three 1.7B task families … isolated venv, no serving/streaming/perf claims yet" (CN equivalent), each linking the new txt+json AND retaining the prior 0.28.0 snapshot link — accurate, no overclaim (the row label "vLLM-Omni serving" with a 🟡 partial marker and explicit "no serving claims" is honest framing, not a serving claim). evidence/README.md index row (line 97) is accurate end-to-end, including the honest-iteration note and the explicit "No serving/streaming/perf claims; 0.6B vLLM variants not tested; memory not recorded". CHANGELOG entry accurately describes the supersession of the 0.28.0 CustomVoice-only snapshot and scopes to offline-only. Roadmap: does NOT overclaim anywhere; item 1's "Status 2026-09-21" block is correctly dated history. STALE SENTENCE FOUND (reported per instructions, not fixed): README.md lines 413-414 — "Upstream features are listed here when they exist upstream but have no Radeon evidence yet (or, for vLLM-Omni, only the single-configuration feasibility check linked below)." — and its twin README_CN.md lines 365-366 — "（vLLM-Omni 除外 —— 它仅有下方链接的单一配置可行性检查）。" — both predate Task 8 and now understate the evidence (contradicting the capability rows updated in the same commit); additionally roadmap item 3 ("vLLM-Omni offline inference on gfx1151") carries no status line linking the new Task 8 evidence. These are understatements, not overclaims; docs/vllm-omni-rocm.md is a self-dated 2026-09-21 A/B report and needs no update; CHANGELOG.md line 235 "FEASIBLE — single configuration only" is a dated historical entry, correctly left as history.

## Discrepancies (all minor, none blocking)

1. Stale roadmap parenthetical: README.md:413-414 and README_CN.md:365-366 still say vLLM-Omni has "only the single-configuration feasibility check", which now contradicts the same files' updated capability rows. The task's "update stale roadmap wording" sub-item is therefore incomplete (capability rows/index/CHANGELOG were updated; this roadmap intro sentence was not). One-line fix for the implementer or a follow-up task; it understates rather than overstates, so it creates no false claim.
2. The "4 clone failures + truncated codeload tarball" (TLS-flaky github git transport) narrative is asserted in the evidence txt/json but no raw log lines recording those failures survive under .work-vllm (grepped all *.log/*.txt — no matches for clone/tarball/TLS/codeload). Immaterial to validity: provenance was independently confirmed via sha256 against the contents API.
3. The task brief lists "startup/load time" among captures; the evidence records no explicit load/startup times (they are derivable from log timestamps; my verifier rerun measured ~95 s process start → final StagePool shutdown). Memory non-capture IS declared as an honest gap; load time is neither recorded nor declared — a small honesty-of-scope nit.

## Observations (no action required)

- Deterministic pipeline: my fresh rerun produced a bit-identical WAV to the archived evidence (same sha256, same 238124 bytes) — the strongest form of independent confirmation available; also implies fixed seeds/sampling in this example path.
- venv30 additionally contains a CPU `onnxruntime 1.30.0` distribution alongside `onnxruntime-rocm 1.22.2.post3`; `import onnxruntime` resolves to 1.22.2 (the rocm build) with `get_available_providers() == ['AzureExecutionProvider', 'CPUExecutionProvider']` — no ROCmExecutionProvider is exposed by this build; the runs succeed regardless (the code2wav stage evidently runs fine on the available providers). Worth knowing for future serving/perf tasks, not a Task 8 defect.
- `import vllm; vllm.__version__` prints `0.30.0` (local segment stripped) while dist metadata says `0.30.0+rocm723` — the evidence correctly records the dist version.
- The implementer's handoff contained a malformed first rendering of the SHA (duplicated segment); the corrected 40-char string is what all committed artifacts record, and it is verified real upstream.
- Official .venv confirmed clean of any vllm packages and `qwen_tts` still imports there; no tracked files were modified during this verification (git status unchanged; untracked .work-docker/ pre-existed).
