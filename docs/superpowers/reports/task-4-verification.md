## VERIFICATION REPORT — TASK 4
### Verdict
PASS

### Acceptance criteria
* [x] **1. Official fine-tuning workflow used.** Phase 4.4 ran upstream `prepare_data.py` AS-IS (clone still pristine at 00:07:23; the sdpa edit happened later at 00:08:13) with exactly upstream README §2's flags (`--device cuda:0 --tokenizer_model_path … --input_jsonl … --output_jsonl …`, transcript line 1463). Phase 4.5 attempted `sft_12hz.py` AS-IS first (verbatim ImportError, line 1545), then trained via `.work-finetune/run_sft_launcher.py`, which I read: it only sets `sys.argv` and calls the imported official module's `train()` verbatim (no re-implemented training logic, no monkey-patching) — not a forked trainer. The only deviation is the R1 one-line `flash_attention_2`→`sdpa` at sft_12hz.py:51, recorded in the transcript (exact 1-line git diff) and in a dedicated section of `docs/finetuning-rocm.md`.
* [x] **2. Real ROCm training steps executed.** Transcript lines 1593–1594 verbatim: `Epoch 0 | Step 0 | Loss: 13.6470` / `Epoch 1 | Step 0 | Loss: 15.0211` (format matches upstream's own `accelerator.print` at sft_12hz.py:124; loss prints only at `step%10==0`, hence two lines). The 12-optimizer-step claim is consistent with the pristine upstream code I read: 12 samples / batch 2 = 6 microbatches/epoch × 2 epochs, `optimizer.step()`/`zero_grad()` ungated at lines 120–121. Real GPU evidenced by `AMD Radeon 8060S Graphics`, 18.024 GiB peak, 19.0 s wall.
* [x] **3. Checkpoint artifact exists.** Inspected `.work-finetune/runs/smoke/checkpoint-epoch-{0,1}/`: epoch-1 has `config.json` (4,538 B), `model.safetensors` (3,833,402,520 bytes; header parses: 404 tensors, `talker.model.codec_embedding.weight` present, all `speaker_encoder*` keys dropped — exactly the upstream conversion) plus tokenizer files.
* [x] **4. Checkpoint reloaded via the OFFICIAL path; sane inference.** `reload_check.py` uses `qwen3_tts_rocm.loader.load(ckpt)` → `qwen_tts.Qwen3TTSModel.from_pretrained(...)` (loader.py:299); converted config verified on disk: `tts_model_type=custom_voice`, `talker_config.spk_id={'smoke_speaker': 3000}`; transcript: supported speakers `['smoke_speaker']`, synthesis 6.1 s, sr 24000, 3.52 s, `assert_wav_sane` True. I independently recomputed the archived `reload_synth_en01.wav`: frames 84480, sr 24000, dur 3.5200 s, peak 0.2158, rms 0.03778, all finite — **matches the claimed values exactly** (and md5 differs from every dataset wav; equal byte-size with utt_en03.wav is a frame-count coincidence).
* [x] **5. Raw evidence archived.** `evidence/finetune-smoke-2026-09-20.txt` (1,715 lines, `=== PHASE ===` separators, staged copy byte-identical to disk, md5 b97a680a…) contains: pinned-clone attempts + bootstrap proofs, inspection recorded before any run (phase 4.2, 00:06:03, before dataset/training), dataset renders, prep (`prepare-exit=0`, `SCHEMA-OK`), the AS-IS flash-attn traceback verbatim, the 1-line diff, loss lines, launcher CUDA stats, reload, zero-patch closeout. The `.json` parses and carries every required field (model, precision, GPU, ROCm/hip 7.14.60850, torch 2.12.0+rocm7.14.0, qwen-tts 0.1.1, pinned SHA, batch/args, seq limits, 12 steps, wall 19.0/24.3 s, peak 18.024/18.486 GiB, both loss lines verbatim, scope_guard) — machine facts, no interpretation.
* [x] **6. No unsupported quality claim.** `docs/finetuning-rocm.md` PROVEN list contains the required items (preprocessing works / training runs 12 steps / checkpoints save / checkpoint reloads via official path / sane post-finetune inference, + dataset build); NOT PROVEN list is exactly the six required (speaker-similarity / convergence / hyperparameters / long-run stability / multi-speaker / production scalability). README + README_CN capability rows are execution-scoped ("✅ scoped — execution-only smoke … no quality/convergence claims"). My independent vocabulary sweep (improv/better/natural/quality/MOS/production-ready/…) over every staged file found only negations inside NOT-PROVEN and pre-existing RTF text ("lower is better") outside the Task 4 additions.
* [x] **7. Zero upstream patches.** Staged diff touches 11 files, none upstream (grep for `target_speaker_embedding`, `class TTSDataset`, `forward_sub_talker_finetune`, `BATCH_INFER_NUM`, etc. → only the hunk header of the *recorded* 1-line diff inside the evidence transcript). `.upstream/Qwen3-TTS`: `rev-parse HEAD` == pinned SHA, `status --porcelain` EMPTY, `diff --stat HEAD` empty, fsck clean. pip RECORD of installed `qwen_tts-0.1.1`: 26/26 files hash-verified, 0 problems — INTACT.

### Code reviewed
Full staged diff (11 files, +2,504/−6): `.gitignore` (+`.upstream/`, `.work-finetune/`, `voices/` — check-ignore verified incl. nested paths), `README.md`/`README_CN.md` (test-count 290/290 refresh + capability row), `CHANGELOG.md` (Task 4 entry, claims-consistent), `docs/finetuning-rocm.md` (239 lines: environment, disclosed bootstrap, disclosed flash-attn workaround, reproduction, PROVEN/NOT PROVEN, limitations), `evidence/README.md` (2 index rows), the two evidence files, `scripts/make_finetune_dataset.py` (217 lines — renders via `loader.load("custom-voice")`, `torch.manual_seed(1234)` per render, `max_new_tokens=512`, `assert_wav_sane` per wav, JSONL schema `audio/text/ref_audio`), `tests/data/finetune_manifest.json` (12 texts + reference), `tests/test_finetune_dataset.py` (8 CPU tests incl. distinctness-from-multilingual). No `src/` changes → zero regression surface. Also reviewed gitignored scratch: `.upstream/Qwen3-TTS/finetuning/sft_12hz.py` (pristine upstream, mechanics match ground-truth §4 exactly), `.work-finetune/run_sft_launcher.py`, `.work-finetune/reload_check.py` — both benign wrappers as claimed.

### Tests executed
Exact commands and exit codes (all run by me, cwd repo root):
- `.venv/bin/python -m pytest -m 'not gpu' -q` → **252 passed, 38 deselected, 15.83s, exit 0** (matches claim)
- `.venv/bin/python -m pytest tests/test_finetune_dataset.py -q` → **8 passed, 0.03s, exit 0**
- `.venv/bin/python -m pytest tests/test_official_demo_parity.py -q` → **3 passed, 13.26s, exit 0**
- `.venv/bin/python -m pytest -m gpu -q` → **38 passed, 252 deselected, 310.73s, exit 0** (real gfx1151; 252+38=290 matches the README refresh)
- `git -C .upstream/Qwen3-TTS rev-parse HEAD` → `022e286b98fbec7e1e916cb940cdf532cd9f488e`, exit 0
- `git -C .upstream/Qwen3-TTS status --porcelain` → empty, exit 0; `diff --stat HEAD | wc -l` → 0; `fsck` clean
- `git ls-remote https://github.com/QwenLM/Qwen3-TTS HEAD` → **`022e286b98fbec7e1e916cb940cdf532cd9f488e	HEAD`, exit 0 — github.com WAS reachable from my session and the SHA matches the pin exactly** (the implementer's blocked-network case did not hold for me; live confirmation supersedes the content-addressing argument)
- JSON parse + required-field check of `evidence/finetune-smoke-2026-09-20.json` → valid JSON, all required fields present (script-checked 21 field paths, 0 missing)
- grep of staged diff for upstream code → clean; manifest-vs-multilingual text overlap → NONE (13 unique texts incl. reference); `train_raw.jsonl` texts == manifest texts in order
- pip RECORD hash audit of `qwen_tts-0.1.1.dist-info/RECORD` → 26 files checked, 0 mismatches/missing

### Runtime evidence
- **Clone authenticity (special adversarial check):** (a) HEAD == pinned SHA ✓; (b) pristine ✓; (c) ls-remote reachable from my session, SHA matches ✓; (d) spot-compare: `sft_12hz.py`, `prepare_data.py`, `finetuning/README.md` byte-IDENTICAL to raw.githubusercontent.com at the pinned SHA; `dataset.py` raw fetch timed out mid-transfer (intermittent network, same symptom the implementer hit), so I compared blob hashes instead — full-tree comparison against the live GitHub API tree: **37/37 blobs match, 0 missing/extra/mismatched**, tree hash `33e4644dc5a698874fa035630d1878aa453b564c` as recorded. The reconstructed commit is the genuine upstream commit.
- Checkpoint + dataset trees intact on disk (see criteria 3–4); `reload_synth_en01.wav` metrics independently reproduced from the archived file.
- Live env re-check: torch 2.12.0+rocm7.14.0, hip 7.14.60850, qwen-tts 0.1.1 — match the JSON/docs.
- Repo boundary: HEAD still `d5a74b0` (Task 3) — no commit made; `git status` shows exactly the 11 staged files, worktree == index; scratch dirs ignored.

### Claims audit
Every headline claim traced: "official workflow end-to-end" → backed (prep AS-IS exit 0, official train(), official from_pretrained reload); "12 optimizer steps" → code-derived, correctly labeled with the upstream per-microbatch quirk; "19.0 s / 18.02 GiB" → transcript lines 1595/1597; "3.52 s / 24 kHz / non-silent" → independently recomputed from the wav; "loss values verbatim, no interpretation" → true in txt, json, docs, CHANGELOG; "zero-patch audit clean" → re-run by me, empty; "github.com blocked at run time" → plausible (my own raw.githubusercontent fetch also timed out intermittently) and now moot — live ls-remote matches the pin; "290/290" → reproduced (252 CPU + 38 GPU). No overclaim found in any staged file. Rulings R1/R2/R3 each followed (R1 sequence as-is→capture→1-line sdpa in clone→restore, recorded in evidence AND docs; R2 no vendoring, ignores effective; R3 12 original self-generated utterances, transcripts == manifest texts, distinct from Task 2 corpus).

### Regression audit
No `src/` or existing-test modifications in the diff; only additive files. CPU suite grew 244→252 (the 8 new dataset tests) with zero failures; GPU suite unchanged at 38/38 — no regression.

### Problems found
1. Transcript line 108: canned label `porcelain-exit=0 (empty output above = pristine)` directly follows a NON-empty 37-line staged-file listing (mid-bootstrap state before the commit object existed). The final audits (lines ~899–902, 1647–1654, phase 4.10) and my live check are correct and empty — but this intermediate label is false as written and is not covered by the machine-appended clarification note.
2. Transcript line 1546: `as-is-run-exit=0` labels the AS-IS flash-attn failure — `$?` came from the `| tail -40` pipe (line 1505), not the failed python process. The traceback and the JSON (which records the run as failed) are accurate; the label is misleading and also not covered by the clarification note.
3. The MIOpen-filtering disclosure is imprecise: `evidence/README.md` and the report state the `grep -v` filter "is itself part of each recorded command line", but the recorded command lines for phases 4.4–4.6 (1463, 1505, 1570) show no `grep -v` — line 1571's own note ("command shown includes the grep -v") contradicts line 1570. Filtering is disclosed and phase 4.3 was left unfiltered, but the "part of the command line / nothing edited after the fact" property is not verifiable from the transcript itself.
4. Phase 4.4/4.5 recorded commands abbreviate paths as `<models/...>`, `<.work-finetune/...>` placeholders — flags are verbatim, paths are not; "Command (verbatim in transcript)" (report table) is therefore loosely true.
5. `run_sft_launcher.py` docstring quotes the brief as prescribing "wrap with a small launcher…" — that sentence is not in `task-4-brief.md` (the brief instead says peak memory from `torch.cuda.max_memory_allocated` if hooks exist, else rocm-smi). Gitignored scratch, not in the commit; the launcher approach itself is disclosed in docs and benign.
6. Cosmetic: JSON `sha_verification` contains a fuzzy timestamp "2026-09-20T23:2x"; JSON/docs cite the brief's step window as "3-50" where the brief says "~10–50" (12 qualifies either way); pre-existing README CI-count inconsistency (215 vs 243) intentionally left for Task 5.

### Final justification
All seven acceptance criteria verified against artifacts I inspected and tests I executed myself, not against the implementer's claims. The highest-risk item — clone authenticity — resolved in the strongest possible way: github.com was reachable from my session, `git ls-remote` returns exactly the pinned SHA, and the local tree matches the live GitHub API tree blob-for-blob (37/37). The fine-tuning run is genuine: the official scripts' mechanics (ungated per-microbatch optimizer, step%10 loss prints, checkpoint conversion with spk_id 3000) all match the pristine upstream source I read, and the archived reload waveform reproduces the published metrics to the last digit. The only deviations from the brief are disclosed (network-forced bootstrap, launcher wrapper for CUDA stats, sdpa reload attention), and no quality/convergence language leaked into any committed file. The six problems found are transcript-labeling and disclosure-precision defects that falsify no criterion; they should be noted for the record (and ideally folded into Task 5's wording sweep) but do not block PASS.
