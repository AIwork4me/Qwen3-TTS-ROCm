# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Packaging artifacts (python -m build output)
/build/
/dist/

# Virtual environments
.venv/

# Downloaded model artifacts (fetched at runtime, never committed)
models/

# Generated spikes evidence binaries are kept only if small; keep wav/tmp out
*.wav

# EXCEPT the bundled synthetic reference clip (Task 13): license-clean, fully
# reproducible via scripts/make_ref_wav.py, needed by the clone/tokenizer suites.
!src/qwen3_tts_rocm/demo/assets/ref_en.wav

# OS/editor
.DS_Store
